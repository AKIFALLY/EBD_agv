# 核心開發原則

## 🎯 適用場景
- AI Agent 進行代碼開發和修改時的核心指導原則
- 確保所有開發都基於實際代碼而非推測
- 提供系統性的代碼分析和理解方法

## 📋 核心開發原則

### 基於實際程式碼開發
**⚠️ 重要：程式開發時必須遵循以下核心原則**

#### 追蹤變數來源
- **查看 import 語句**: 遇到不明變數時，必須查看 `import` 語句找出定義來源
- **搜尋相關檔案**: 使用 `rg` 工具深入搜尋變數定義和使用範例
- **理解後行動**: 完全理解變數意義、用途和上下文後才進行修改
- **禁止推測**: 絕不使用推測或預想的內容，只使用實際程式內的定義
- **禁止虛構**: 絕不編造不存在的功能、檔案、配置或接口
- **先檢查再撰寫**: 如果不能確定某內容是否真實存在，必須先使用工具檢查
- **只寫真實存在**: 只有經過驗證真實存在的東西才能寫入文檔和README

#### 實施方法
```bash
# 1. 追蹤變數定義
rg "variable_name.*=" --type py -C 3        # 查找變數定義
rg "import.*variable_name" --type py        # 查找導入來源
rg "from.*import.*variable_name" --type py  # 查找具體導入

# 2. 理解變數上下文
rg "variable_name" --type py -C 5           # 查看使用上下文
rg "class.*variable_name|def.*variable_name" --type py  # 查找相關類/函數

# 3. 驗證理解正確性
# 在多個檔案中確認變數一致性使用
# 檢查型別提示和文檔字串

# 4. 驗證內容真實性
ls -la /path/to/file                    # 確認檔案確實存在
rg "function_name|class_name" --type py # 確認函數/類確實存在
cat /path/to/config.yaml               # 確認配置項確實存在
# ROS 2 接口驗證需在容器內執行：
# docker compose -f docker-compose.agvc.yml exec agvc_server bash -c "source /app/setup.bash && ros2 interface show msg_type"
```

### 環境識別和工具使用
- **環境確認**: 透過 `pwd` 命令輸出判斷當前環境（`/home/ct/RosAGV` 為宿主機，`/app` 為容器內）
- **統一工具**: 在宿主機使用 `r` 命令進行系統診斷和容器管理
- **專業工具**: 在容器內使用專業工具集進行開發和測試

### 搜尋工具優先級
**⚠️ 重要：AI Agent 必須優先使用高效能搜尋工具**

- **優先使用 `rg` (ripgrep)**: 代替傳統的 `grep` 指令，提供更快的搜尋效能
- **Grep 工具**: 使用 Claude Code 提供的 Grep 工具進行代碼搜尋
- **避免使用 `grep`**: 在文檔和指令範例中應使用 `rg` 而非 `grep`
- **效能考量**: `rg` 在大型代碼庫中的搜尋速度明顯優於 `grep`

#### 工具使用對照
```bash
# ✅ 推薦：使用 rg
rg "pattern" --type py          # 搜尋 Python 檔案
rg "pattern" -C 3               # 顯示上下文
rg "CREATE USER|CREATE DATABASE" --type sh --type sql

# ❌ 避免：使用 grep
grep -r "pattern" --include="*.py"
grep "pattern" **/*.py
```

### 結構化資料處理工具
**⚠️ 重要：優先使用專業工具處理結構化資料**

- **`jq` 用於 JSON 處理**: 容器內已安裝，處理標準 JSON 配置檔案
- **`json5` + `jq` 用於 JSON5 處理**: JSON5 需要先轉換為 JSON 再用 jq 處理
- **`yq` 用於 YAML 處理**: 容器內已安裝，處理 YAML 配置檔案的專業工具
- **避免使用 `cat` + 手工解析**: 結構化資料應使用專業工具處理

#### JSON5 vs JSON 重要差異
**RosAGV 中的 `/app/routerconfig.json5` 使用 JSON5 格式，具有以下特性：**

| 特性 | JSON | JSON5 | RosAGV 範例 |
|------|------|-------|-------------|
| **註解支援** | ❌ 不支援 | ✅ `//` 和 `/* */` | `// Zenoh Router 配置` |
| **物件鍵引號** | ❌ 必須有引號 | ✅ 可省略引號 | `mode: "router"` |
| **尾隨逗號** | ❌ 不允許 | ✅ 允許 | `endpoints: [...,]` |
| **多行字串** | ❌ 不支援 | ✅ 支援 | 複雜配置說明 |
| **數字格式** | 🔒 嚴格 | 🔓 寬鬆 | 支援 `0x`, `Infinity` |
| **工具處理** | `jq file.json` | `json5 file.json5 \| jq` | 必須先轉換 |

#### JSON/JSON5 處理範例
**⚠️ 重要：區分 JSON 和 JSON5 格式的處理方法**

```bash
# ✅ 正確：處理 JSON5 檔案 (如 routerconfig.json5)
json5 /app/routerconfig.json5 | jq '.mode'                    # 查看模式配置
json5 /app/routerconfig.json5 | jq '.listen.endpoints[]'      # 查看監聽端點
json5 /app/routerconfig.json5 | jq '.transport.unicast.lowlatency'  # 查看效能配置

# ✅ JSON5 格式驗證
json5 --validate /app/routerconfig.json5                      # 驗證 JSON5 語法

# ✅ 處理標準 JSON 檔案
jq '.key' file.json                                           # 直接用 jq 處理 JSON

# ✅ JSON5 轉 JSON 並保存
json5 /app/routerconfig.json5 | jq . > config.json

# ❌ 錯誤：直接用 jq 處理 JSON5
jq '.mode' /app/routerconfig.json5                            # 會失敗！JSON5 有註解和無引號鍵

# ❌ 避免：手工解析結構化資料
cat /app/routerconfig.json5 | grep "mode"
```

**JSON5 vs JSON 識別方法：**
- 副檔名 `.json5` → 使用 `json5 file.json5 | jq`
- 副檔名 `.json` → 直接使用 `jq . file.json`
- 檔案有註解 `//` 或 `/* */` → JSON5 格式
- 物件鍵沒有引號 → JSON5 格式

#### JSON5 工具完整使用指導

```bash
# 🔍 JSON5 檔案識別和驗證
file /app/routerconfig.json5                              # 檢查檔案類型
head -5 /app/routerconfig.json5                          # 查看檔案開頭 (有註解即為 JSON5)
json5 --validate /app/routerconfig.json5                 # 驗證 JSON5 語法

# 📖 JSON5 內容查看和分析
json5 /app/routerconfig.json5 | jq . | head -20         # 預覽結構
json5 /app/routerconfig.json5 | jq -C . | less          # 彩色分頁顯示
json5 /app/routerconfig.json5 | jq 'keys' | sort        # 查看所有配置項

# 🔄 JSON5 ↔ JSON 轉換
json5 /app/routerconfig.json5 > /tmp/config.json        # JSON5 → JSON
json5 --space 2 /app/routerconfig.json5 > /tmp/config.json  # 格式化輸出

# ⚙️ 配置查詢和修改工作流程
# 1. 備份原檔案
cp /app/routerconfig.json5 /app/routerconfig.json5.backup

# 2. 查詢配置
json5 /app/routerconfig.json5 | jq '.mode'              # 查看特定配置
json5 /app/routerconfig.json5 | jq '.listen'            # 查看監聽配置

# 3. 修改配置 (推薦直接編輯 JSON5)
vim /app/routerconfig.json5                             # 保留註解和格式
# 或使用專用配置工具
scripts/config-tools/zenoh-config.sh edit

# 4. 驗證修改結果
json5 --validate /app/routerconfig.json5                # 語法檢查
json5 /app/routerconfig.json5 | jq empty               # 結構檢查
```

**⚠️ JSON5 修改注意事項：**
- 推薦直接編輯 `.json5` 檔案以保留註解和格式
- 避免 JSON5 → JSON → 修改 → JSON5 的轉換流程（會丟失註解）
- 使用 `json5 --validate` 確保語法正確
- 修改後用組合指令 `json5 file.json5 | jq` 驗證結構

#### YAML 處理範例 (yq)
```bash
# ✅ 推薦：使用 yq 處理 YAML
yq '.services.agvc_server.ports' docker-compose.agvc.yml     # 查看服務端口
yq '.networks' docker-compose.agvc.yml                       # 查看網路配置
yq '.services.*.image' docker-compose.agvc.yml               # 查看所有映像

# ✅ 修改 YAML 配置
yq '.services.agvc_server.ports[0] = "8001:8000"' docker-compose.agvc.yml

# ❌ 避免：手工解析 YAML
cat docker-compose.agvc.yml | grep -A 5 "ports:"
```

### 代碼分析流程
1. **理解需求**: 明確要實現的功能和預期結果
2. **搜尋相關代碼**: 使用 Grep 和 Glob 工具找到相關實現
3. **驗證內容真實性**: 確認所有引用的檔案、函數、類、配置確實存在
4. **分析現有模式**: 理解現有代碼的架構和設計模式
5. **驗證依賴關係**: 確認所有依賴和導入的正確性
6. **實施修改**: 基於理解和驗證進行精確的代碼修改
7. **測試驗證**: 確保修改不會破壞現有功能

### 文檔寫作原則

**📝 文檔撰寫的詳細規範和標準請參考**: docs-ai/operations/development/core/documentation-standards.md

包含以下重要內容：
- 真實性原則（只寫真實存在的內容）
- 時間記錄規範（使用真實系統時間）
- 格式規範和文檔結構
- 可驗證性要求
- 更新維護原則

## 🔧 Bash Tool 使用技巧

### 腳本語法限制和解決方案
在使用 Bash tool 執行複雜腳本時，需要注意以下語法限制：

#### 子命令替換問題
```bash
# ❌ 問題語法：$() 在 Bash tool 中可能無法正確解析
matches=$(rg -l "pattern" . | wc -l)

# ✅ 解決方案：使用反引號替換
matches=`rg -l "pattern" . | wc -l`

# ✅ 或者分步執行
rg -l "pattern" . > /tmp/matches.txt
matches=`wc -l < /tmp/matches.txt`
```

#### 複雜多行腳本處理
```bash
# ❌ 避免：過於複雜的嵌套結構
for file in $(find . -name "*.md"); do
    if [ $(rg -l "@docs-ai" "$file" | wc -l) -gt 0 ]; then
        echo "Found: $file"
    fi
done

# ✅ 建議：分解為簡單步驟
find . -name "*.md" > /tmp/files.txt
for file in `cat /tmp/files.txt`; do
    matches=`rg -l "@docs-ai" "$file" | wc -l`
    if [ "$matches" -gt 0 ]; then
        echo "Found: $file"
    fi
done
```

#### 特殊字元處理
```bash
# ✅ 正確：使用雙引號保護包含空格的路徑
for file in "docs-ai/README.md" "docs-ai/USAGE_GUIDE.md"; do
    echo "處理檔案: $file"
done

# ✅ 正確：變數替換語法
file="docs-ai/README.md"
relative_path=${file#docs-ai/}  # 移除前綴
echo "相對路徑: $relative_path"
```

#### 錯誤處理和除錯
```bash
# ✅ 建議：加入錯誤處理
command_output=`some_command 2>/dev/null`
if [ $? -eq 0 ]; then
    echo "成功: $command_output"
else
    echo "失敗: 命令執行出錯"
fi

# ✅ 建議：使用 echo 除錯複雜邏輯
echo "開始處理檔案: $file"
echo "模式: $pattern"
echo "結果: $matches"
```

#### 臨時腳本檔案最佳實踐
```bash
# ✅ 建議：複雜指令建立臨時腳本檔案執行
# 在宿主機 agents 目錄下建立臨時腳本
cat > ~/RosAGV/agents/temp.sh << 'EOF'
#!/bin/bash
# 複雜的命令序列
echo "開始執行複雜操作..."
for i in {1..5}; do
    echo "步驟 $i"
done
echo "操作完成"
EOF

# 在宿主機執行
chmod +x ~/RosAGV/agents/temp.sh
bash ~/RosAGV/agents/temp.sh

# 在容器內執行 (對應路徑: /app/agents/temp.sh)
docker exec -it container_name bash /app/agents/temp.sh
```

#### 臨時腳本的目錄對應關係
- **宿主機路徑**: `~/RosAGV/agents/temp.sh`
- **容器內路徑**: `/app/agents/temp.sh`
- **統一管理**: 所有臨時檔案都放在 agents/ 目錄，便於管理和清理
- **使用場景**: 複雜的多行指令、需要錯誤處理的腳本、重複執行的操作

#### Interactive Bash Mode 解決方案
**⚠️ 重要發現**: 當 Bash tool 遇到容器指令超時或 alias 載入問題時，使用 `bash -i` 參數：

```bash
# ✅ 推薦解決方案: bash -i (interactive flag)
docker compose -f docker-compose.agvc.yml exec agvc_server bash -i -c "source /app/setup.bash && agvc_source && manage_web_api_launch stop && ba && sa && manage_web_api_launch start"

# 原理: 互動式模式確保 alias 和環境正確載入
# 應用: Web 服務管理、建置工作流、系統重啟等複雜指令序列
```

**詳細說明**: docs-ai/operations/development/docker-development.md#容器指令執行技巧

### 最佳實踐
1. **簡化邏輯**: 避免過於複雜的嵌套和管道操作
2. **分步執行**: 將複雜操作分解為多個簡單步驟
3. **使用反引號**: 優先使用 `` ` `` 而非 `$()`
4. **臨時腳本**: 複雜指令建立 temp.sh 或 temp.bash 檔案執行
5. **添加除錯輸出**: 在關鍵步驟添加 echo 除錯資訊
6. **錯誤處理**: 檢查命令執行結果和退出碼

## 📋 創建可重用腳本原則

### 腳本創建策略
**⚠️ 重要：當發現常用指令或查詢條件經常重複使用時，應該創建可重用腳本**

- **識別重複模式**: 當某個指令序列或查詢條件被使用3次以上時，考慮創建腳本
- **建立 .sh 檔案**: 在 `scripts/` 目錄下創建對應的 shell 腳本
- **記錄到 docs-ai**: 將新創建的腳本功能和使用方法記錄到 docs-ai 文檔中
- **團隊共享**: 讓其他 AI Agent 和人類夥伴都能使用這些實用工具

### 腳本創建工作流程
```bash
# 1. 識別重複指令模式
# 當你發現經常使用類似的指令時，例如：
rg "CREATE USER|CREATE DATABASE" --type sh --type sql
find . -name "*init*" -o -name "*database*"
docker compose -f docker-compose.agvc.yml logs agvc_server | rg -i "error"

# 2. 創建腳本檔案
# 在 scripts/ 對應分類目錄下創建 .sh 檔案
cat > scripts/database-tools/db-search.sh << 'EOF'
#!/bin/bash
# 資料庫相關檔案和配置搜尋工具
echo "搜尋資料庫相關檔案..."
rg "CREATE USER|CREATE DATABASE" --type sh --type sql
find . -name "*init*" -o -name "*database*" -o -name "scripts"
EOF

chmod +x scripts/database-tools/db-search.sh

# 3. 測試腳本功能
./scripts/database-tools/db-search.sh

# 4. 記錄到 docs-ai
# 在對應的文檔中記錄新腳本的功能和使用方法
```

### 腳本分類目錄
根據功能將腳本組織到對應目錄：
- **config-tools/**: 配置管理相關腳本
- **dev-tools/**: 開發工作流相關腳本
- **docker-tools/**: 容器管理相關腳本
- **log-tools/**: 日誌分析相關腳本
- **network-tools/**: 網路診斷相關腳本
- **system-tools/**: 系統診斷相關腳本
- **database-tools/**: 資料庫操作相關腳本 (如需要)

### 腳本命名規範
- **功能描述性**: 腳本名稱應清楚描述其功能
- **使用 kebab-case**: 使用連字符分隔單詞，如 `db-search.sh`
- **包含動作**: 名稱應包含動作詞，如 `check-`, `search-`, `init-`
- **避免縮寫**: 除非是廣泛認知的縮寫，否則使用完整單詞

### 腳本文檔化要求
每個新創建的腳本都應該：
1. **包含註解**: 在腳本開頭說明功能和使用方法
2. **更新工具索引**: 在相關的 docs-ai 文檔中記錄新工具
3. **提供使用範例**: 在文檔中提供具體的使用範例
4. **說明適用場景**: 描述何時應該使用這個腳本

### 實踐範例
```bash
# 範例：常用的容器日誌錯誤搜尋
# 發現這個模式經常使用：
docker compose -f docker-compose.agvc.yml logs agvc_server | rg -i "error|exception|failed"

# 創建可重用腳本：
cat > scripts/log-tools/agvc-error-scan.sh << 'EOF'
#!/bin/bash
# AGVC 容器錯誤日誌快速掃描工具
# 用法: ./agvc-error-scan.sh [service_name]

SERVICE=${1:-agvc_server}
echo "掃描 $SERVICE 服務的錯誤日誌..."
docker compose -f docker-compose.agvc.yml logs $SERVICE | rg -i "error|exception|failed|timeout" --color=always
EOF

chmod +x scripts/log-tools/agvc-error-scan.sh

# 記錄到 docs-ai/operations/tools/unified-tools.md 中
```

### 腳本維護原則
1. **定期檢查**: 定期檢查腳本是否仍然有效和實用
2. **版本更新**: 當系統變更時，及時更新相關腳本
3. **清理過時**: 移除不再使用或過時的腳本
4. **功能整合**: 將相似功能的腳本整合為更通用的工具

## 🚨 AI Agent 執行限制

### AI Agent 的實際能力邊界
**⚠️ 重要：了解 AI Agent 的執行限制，避免不切實際的期望**

#### 1. ROS 2 命令執行限制
- **限制**: AI Agent 無法在宿主機直接執行 `ros2` 命令
- **原因**: ROS 2 環境只存在於 Docker 容器內
- **解決方案**: 使用 `docker compose exec` 包裝命令在容器內執行
```bash
# ❌ 無法執行（在宿主機）
ros2 interface show std_msgs/msg/String

# ✅ 正確方式
docker compose -f docker-compose.agvc.yml exec agvc_server bash -c "source /app/setup.bash && ros2 interface show std_msgs/msg/String"
```

#### 2. 環境判斷限制
- **限制**: AI Agent 無法主動"識別"環境，只能被動推斷
- **方法**: 透過命令輸出判斷（如 `pwd` 結果）
- **判斷標準**:
  - `/home/ct/RosAGV` = 宿主機環境
  - `/app` = 容器內環境

#### 3. Bash Tool 語法限制
- **限制**: `$()` 子命令替換在 Bash tool 中可能無法正確解析
- **解決**: 使用反引號 `` ` `` 或分步執行
- **詳見**: 本文檔 "Bash Tool 使用技巧" 章節

#### 4. 檔案系統操作限制
- **限制**: AI Agent 只能透過提供的工具操作檔案
- **可用工具**: Read, Write, Edit, MultiEdit, Bash
- **無法執行**: 直接的檔案系統操作或二進制程式執行

#### 5. 網路操作能力
- **可執行**: 透過 Bash tool 使用 `curl`、`wget` 等命令進行網路操作
- **專用工具**: WebFetch (用於取得並分析網頁內容)
- **範例**: `curl -s https://api.github.com/zen`、`wget -q -O - https://example.com`
- **限制**: 無法直接建立 socket 連線或持久連線，但可透過命令行工具實現大部分網路需求

### AI Agent 工作最佳實踐
1. **依賴工具輸出**: 所有判斷基於工具執行結果
2. **容器指令包裝**: ROS 2 相關命令須透過 docker exec 執行
3. **驗證優先**: 執行前先驗證檔案、路徑、命令是否存在
4. **善用 Bash 能力**: 可使用 `date`、`curl`、`wget` 等命令擴展功能
5. **錯誤處理**: 預期工具可能失敗，提供替代方案
6. **明確限制**: 遇到真正無法執行的操作時，明確告知使用者

## 🔗 交叉引用
- 環境管理: docs-ai/context/system/dual-environment.md
- 工具使用: docs-ai/operations/tools/unified-tools.md