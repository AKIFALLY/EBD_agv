# 文檔撰寫標準與規範

## 🎯 適用場景
- AI Agent 撰寫技術文檔、README、報告時的必要規範
- 確保所有文檔內容真實、準確、可驗證
- 提供文檔格式、內容和時間記錄的標準化指導

## 📋 文檔撰寫核心原則

### 🚨 第一原則：真實性原則
**⚠️ 絕對原則：只有真實存在的內容才能寫進文檔**

#### 內容真實性要求
- **基於實際檢查**: 任何寫入文檔的內容都必須基於實際的工具檢查結果
- **驗證檔案存在**: 使用 `ls`, `LS`, `Read` 等工具驗證檔案確實存在
- **驗證功能存在**: 使用 `Grep`, `rg` 等工具驗證函數、類、配置確實存在
- **避免虛構範例**: 所有程式碼範例、配置範例都必須來自實際檔案
- **禁止推測**: 絕不使用推測或預想的內容，只使用實際程式內的定義
- **禁止編造**: 絕不編造不存在的功能、檔案、配置或接口

#### 內容驗證工作流程
```bash
# 1. 寫入前驗證
ls -la /path/to/file              # 確認檔案存在
rg "class_name|function_name"     # 確認程式碼存在
cat /path/to/config               # 確認配置存在
ros2 interface show msg_type      # 確認 ROS 2 接口存在

# 2. 寫入真實內容
# 只寫入經過驗證的內容

# 3. 定期重新驗證
# 定期重新檢查文檔中引用的內容是否仍然存在
```

### ⏰ 第二原則：時間記錄原則
**⚠️ 重要原則：所有時間戳記必須使用真實的系統時間，禁止編造**

#### 時間記錄要求
- **使用真實時間**: 必須使用系統函數取得當前時間，不可隨意編造
- **台灣時區**: 統一使用 Asia/Taipei 時區 (CST/UTC+8)
- **標準格式**: 使用 ISO 8601 格式 `YYYY-MM-DD HH:MM:SS`
- **包含時區**: 在重要文檔中應包含時區資訊

#### 取得正確時間的方法
```bash
# Shell 命令取得台灣時間
TZ='Asia/Taipei' date '+%Y-%m-%d %H:%M:%S %Z'
# 輸出範例: 2025-08-13 08:46:02 CST

# Python 取得台灣時間
python3 -c "
from datetime import datetime
import pytz
tz = pytz.timezone('Asia/Taipei')
print(datetime.now(tz).strftime('%Y-%m-%d %H:%M:%S %Z'))
"

# 在文檔中記錄時間範例
echo "## 執行日期: $(TZ='Asia/Taipei' date '+%Y-%m-%d')"
echo "## 最後更新: $(TZ='Asia/Taipei' date '+%Y-%m-%d %H:%M:%S')"
```

#### 文檔時間戳記範例
```markdown
# 正確的時間記錄範例
## 執行日期: 2025-08-13
## 最後更新: 2025-08-13 08:46:02 CST
## 建立時間: 2025-08-13 08:30:00

# ❌ 錯誤範例 (編造的時間)
## 執行日期: 2025-01-12  # 如果實際是 8 月卻寫 1 月
## 最後更新: 2025-XX-XX  # 使用 XX 或其他佔位符（這是錯誤範例）
```

### 📌 第三原則：引用語法規範原則
**⚠️ 重要原則：區分強引用與弱引用的使用場景**

#### 引用格式規則
- **弱引用 (推薦)**: `docs-ai/` - 用於 README.md 和所有文檔內容
- **強引用 (@)**: `@docs-ai/` - 僅用於 CLAUDE.md 文件中（可選）

#### 使用場景規範
| 檔案類型 | 引用格式 | 範例 |
|---------|---------|------|
| README.md | 弱引用 | `docs-ai/context/system/rosagv-overview.md` |
| docs-ai/ 下所有 .md | 弱引用 | `docs-ai/operations/guides/troubleshooting.md` |
| 一般技術文檔 | 弱引用 | `docs-ai/knowledge/protocols/ros2-interfaces.md` |
| CLAUDE.md | 強引用或弱引用 | `@docs-ai/` 或 `docs-ai/` （視需求選擇） |

#### 理由說明
- **弱引用優先**：避免過度依賴特定語法，提高文檔可讀性和通用性
- **CLAUDE.md 特例**：作為 AI Agent 記憶文件，支援 @ 強引用以便系統識別和載入
- **一致性原則**：在同一文檔內保持引用格式一致
- **可移植性**：弱引用格式更容易被其他系統和工具識別

#### 引用範例
```markdown
# ✅ 正確範例 - README.md 中
詳見：docs-ai/operations/development/docker-development.md
參考：docs-ai/knowledge/agv-domain/vehicle-types.md

# ✅ 正確範例 - CLAUDE.md 中（兩種都可）
@docs-ai/context/system/dual-environment.md
docs-ai/context/system/dual-environment.md

# ❌ 錯誤範例 - README.md 中使用強引用
@docs-ai/operations/guides/troubleshooting.md  # 應使用弱引用
```

### 📝 第四原則：格式規範原則

#### 文檔結構要求
1. **標題層級**: 使用清晰的 Markdown 標題層級 (# ## ### ####)
2. **章節編號**: 重要文檔應有清晰的章節編號
3. **目錄索引**: 長文檔應包含目錄 (Table of Contents)
4. **交叉引用**: 使用 `docs-ai/` 格式引用其他文檔

#### 必要元資訊
每個重要文檔應包含：
```markdown
# 文檔標題

## 🎯 適用場景
- 說明文檔的使用場景和目標讀者

## 📋 文檔概述
簡要說明文檔內容

## 執行資訊
- **建立日期**: [使用真實時間]
- **最後更新**: [使用真實時間]
- **作者**: AI Agent / 人類開發者名稱
- **版本**: v1.0

## 主要內容
[文檔主體]

## 🔗 交叉引用
- 相關文檔連結
```

### 🔍 第五原則：可驗證性原則

#### 內容可驗證要求
- **提供驗證命令**: 對於技術內容，提供驗證命令
- **包含實際輸出**: 包含命令的實際執行結果
- **標註來源**: 標註資訊的來源檔案或命令

#### 驗證範例
```markdown
# 可驗證的文檔範例

## 系統配置
根據實際檢查 (執行時間: 2025-08-13 08:46:02):

\`\`\`bash
# 驗證命令
cat /app/config/system.yaml

# 實際輸出
system:
  version: "2.0"
  environment: "production"
\`\`\`

資料來源: `/app/config/system.yaml`
```

### 📊 第六原則：更新維護原則

#### 文檔更新要求
- **同步更新**: 當程式碼變更時，相關文檔必須同步更新
- **版本記錄**: 記錄每次重要更新的時間和內容
- **過期標註**: 對可能過期的內容加註時間戳記

#### 更新記錄範例
```markdown
## 更新歷史
| 日期 | 版本 | 更新內容 | 更新者 |
|------|------|---------|--------|
| 2025-08-13 | v1.0 | 初始版本 | AI Agent |
| 2025-08-13 | v1.1 | 新增時間記錄規範 | AI Agent |
```

## 🚫 禁止事項清單

### 絕對禁止
1. ❌ **編造時間**: 隨意寫入不真實的日期時間
2. ❌ **虛構功能**: 描述不存在的功能或檔案
3. ❌ **推測內容**: 基於推測而非實際檢查的內容
4. ❌ **佔位符**: 使用 XXX、TODO、TBD 等佔位符代替真實資訊
5. ❌ **過時資訊**: 保留已知過時或錯誤的資訊

### 應當避免
1. ⚠️ 過於冗長的描述
2. ⚠️ 缺乏實際範例
3. ⚠️ 沒有驗證方法
4. ⚠️ 缺少交叉引用
5. ⚠️ 格式不一致

## 📋 文檔類型規範

### 技術文檔
- **README.md**: 專案概述、安裝、使用說明
- **CLAUDE.md**: AI Agent 專用記憶文檔
- **API 文檔**: 介面說明、參數、範例
- **架構文檔**: 系統設計、模組關係

### 操作文檔
- **安裝指南**: 步驟清晰、可重現
- **故障排除**: 問題、原因、解決方案
- **維護手冊**: 定期任務、檢查清單

### 報告文檔
- **測試報告**: 測試時間、結果、結論
- **錯誤報告**: 發生時間、錯誤詳情、解決方案
- **進度報告**: 時間範圍、完成事項、待辦事項

## 🔧 實作指導

### 創建新文檔時的檢查清單
```bash
# 1. 確定文檔位置
ls -la /home/ct/RosAGV/docs-ai/operations/development/

# 2. 取得當前時間
current_time=$(TZ='Asia/Taipei' date '+%Y-%m-%d %H:%M:%S')
echo "當前時間: $current_time"

# 3. 驗證引用內容
rg "要引用的函數或類別" --type py
ls -la "要引用的檔案路徑"

# 4. 創建文檔並加入元資訊
cat > new_document.md << EOF
# 文檔標題

## 執行資訊
- **建立日期**: $current_time
- **作者**: AI Agent

## 內容
[經過驗證的真實內容]
EOF
```

### 更新現有文檔時
```bash
# 1. 記錄更新時間
update_time=$(TZ='Asia/Taipei' date '+%Y-%m-%d %H:%M:%S')

# 2. 驗證變更內容
# 檢查相關程式碼是否真的變更

# 3. 更新文檔並記錄
echo "## 最後更新: $update_time" >> document.md
```

## 🔗 交叉引用
- 核心開發原則: docs-ai/operations/development/core/core-principles.md
- 測試標準: docs-ai/operations/development/testing/testing-standards.md
- 語言配置: docs-ai/context/system/language-configuration.md

---
*文檔建立時間: 2025-08-13 08:50:00 CST*
*最後更新: 2025-08-13 08:50:00 CST*
*版本: v1.0*