# RosAGV 根目錄 AI 開發指導文檔

## ⚠️ 重要：執行環境識別
**執行環境**: 宿主機 (Ubuntu 24.04)
**目標用戶**: 系統管理員、DevOps 工程師、專案經理、AI 助理
**最後更新**: 2025-01-17

### 🔍 當前環境確認
**此文檔適用於容器外的部署和管理工作**

**環境識別特徵**:
- 當前工作目錄：`/home/ct/RosAGV`
- 可執行：`docker compose`、`git`、`sudo`
- 無法直接執行：`ros2`、`colcon`（需進入容器）
- 檔案結構：可看到 `docker-compose.yml`、`Dockerfile` 等

**指令執行方式**:
```bash
# ✅ 正確：宿主機執行 Docker 指令
docker compose -f docker-compose.yml up -d
docker compose -f docker-compose.yml exec rosagv bash
docker compose -f docker-compose.agvc.yml logs -f agvc_server

# ❌ 錯誤：不要在宿主機直接執行容器內指令
ros2 launch loader_agv launch.py  # 這是容器內指令
colcon build                      # 這是容器內指令
source setup.bash               # 這是容器內指令
```

**如果您在容器內環境**，請參考應用目錄的 `app/.augment-guidelines` 文檔。

## 🏗️ 專案整體開發規範

### Python 執行環境
**重要**: 在 Ubuntu 24.04 環境下，必須使用 `python3` 而非 `python`

**宿主機環境中的 Python 指令**:
```bash
# ✅ 正確：在宿主機使用 python3
python3 --version
python3 -m pip install package

# 透過 Docker 執行 Python 指令
docker compose -f docker-compose.yml exec rosagv python3 script.py
docker compose -f docker-compose.agvc.yml exec agvc_server python3 -m pytest
```

**容器內環境中的 Python 指令**:
```bash
# ✅ 正確：在容器內使用 python3
python3 script.py
python3 -m pip install package
python3 -c "import sys; print(sys.version)"

# ROS 2 Python 節點
ros2 run package_name python_node
python3 /app/workspace/src/package/script.py
```

**套件管理最佳實踐**:
```bash
# 使用 pip3 而非 pip
pip3 install package_name
pip3 list
pip3 show package_name

# 在 Docker 容器中安裝套件
docker compose -f docker-compose.yml exec rosagv pip3 install package
```

### Python 虛擬環境配置
**Docker 容器內虛擬環境配置**:
- **虛擬環境路徑**: `/opt/pyvenv_env`
- **PYTHONPATH 自動設定**: `/opt/pyvenv_env/lib/python3.12/site-packages`
- **自動載入機制**: 透過容器啟動腳本自動配置

**宿主機環境中的虛擬環境管理**:
```bash
# 檢查容器內虛擬環境狀態
docker compose -f docker-compose.yml exec rosagv bash -c "echo \$PYTHONPATH"
docker compose -f docker-compose.agvc.yml exec agvc_server bash -c "/opt/pyvenv_env/bin/pip3 list"

# 在容器內安裝虛擬環境套件
docker compose -f docker-compose.yml exec rosagv /opt/pyvenv_env/bin/pip3 install package_name
docker compose -f docker-compose.agvc.yml exec agvc_server /opt/pyvenv_env/bin/pip3 install package_name
```

**容器內環境中的虛擬環境管理**:
```bash
# ✅ 正確：使用虛擬環境的 pip
/opt/pyvenv_env/bin/pip3 install package_name
/opt/pyvenv_env/bin/pip3 list

# 檢查虛擬環境套件位置
python3 -c "import fastapi; print(fastapi.__file__)"
python3 -c "import sqlalchemy; print(sqlalchemy.__file__)"

# 環境診斷
echo $PYTHONPATH
echo $VIRTUAL_ENV
```

**虛擬環境套件使用場景**:
- **Web 相關工作空間**: `web_api_ws`, `db_proxy_ws` 等使用 FastAPI、SQLAlchemy
- **一般 ROS 2 工作空間**: `keyence_plc_ws`, `plc_proxy_ws` 等使用系統套件
- **混合使用**: 某些工作空間可能同時使用系統套件和虛擬環境套件

### 雙環境部署策略
RosAGV 採用雙環境容器化架構，每個環境有不同的職責和部署方式：

#### 🚗 AGV 車載系統
**部署位置**: AGV 車輛上的電腦
**容器名稱**: `rosagv`
**Docker Compose**: `docker-compose.yml`
**啟動指令**: `docker compose -f docker-compose.yml up -d`
**職責範圍**:
- AGV 即時控制和狀態管理
- PLC 設備直接通訊
- 感測器資料處理
- 手動控制（搖桿）支援
- 路徑規劃和導航

#### 🖥️ AGVC 管理系統
**部署位置**: AGVC 管理電腦
**容器名稱**: `agvc_server`
**Docker Compose**: `docker-compose.agvc.yml`
**啟動指令**: `docker compose -f docker-compose.agvc.yml up -d`
**職責範圍**:
- 車隊管理和任務調度
- 資料庫管理和資料持久化
- Web 管理介面
- 外部系統整合（KUKA Fleet）
- 系統監控和日誌管理

### Docker Compose V2 容器管理原則

#### 正確的指令語法
```bash
# ✅ 正確：使用 Docker Compose V2 語法
docker compose -f docker-compose.yml up -d
docker compose -f docker-compose.agvc.yml up -d

# ❌ 錯誤：舊版語法（不要使用）
docker-compose -f docker-compose.yml up -d
```

#### 容器生命週期管理
```bash
# 啟動服務
docker compose -f <compose-file> up -d

# 查看狀態
docker compose -f <compose-file> ps

# 查看日誌
docker compose -f <compose-file> logs -f [service_name]

# 進入容器
docker compose -f <compose-file> exec <container_name> bash

# 重啟服務
docker compose -f <compose-file> restart [service_name]

# 停止服務
docker compose -f <compose-file> down

# 強制重建
docker compose -f <compose-file> up -d --build --force-recreate
```

#### 容器網路和資源管理
- 每個環境使用獨立的 Docker 網路
- 共用 Zenoh Router 通訊 (port 7447)
- 資源限制和健康檢查配置
- 持久化資料卷管理

### 版本控制和發布流程

#### Git 工作流程
```bash
# 功能開發分支
git checkout -b feature/new-workspace
git add .
git commit -m "feat: 新增 xxx_ws 工作空間"

# 文檔同步檢查
./scripts/check-docs-consistency.sh

# 合併到主分支
git checkout main
git merge feature/new-workspace
```

#### 發布版本管理
- 使用語義化版本控制 (Semantic Versioning)
- 主要版本：架構重大變更
- 次要版本：新功能新增
- 修補版本：錯誤修復

#### Docker 映像版本管理
```bash
# 建置映像
docker build -f Dockerfile -t rosagv:v1.0.0 .
docker build -f Dockerfile.agvc -t rosagv-agvc:v1.0.0 .

# 標記和推送
docker tag rosagv:v1.0.0 registry/rosagv:v1.0.0
docker push registry/rosagv:v1.0.0
```

## 🚀 部署和運維指導

### 容器啟動順序和依賴關係

#### AGV 車載系統啟動流程
```mermaid
graph TD
    A[Docker Container Start] --> B[startup.agv.bash]
    B --> C[SSH Service]
    B --> D[Zenoh Router]
    B --> E[Load Workspaces]
    E --> F[keyence_plc_ws]
    E --> G[plc_proxy_ws]
    E --> H[agv_cmd_service_ws]
    E --> I[joystick_ws]
    E --> J[agv_ws]
    E --> K[path_algorithm]
    J --> L[ros2 launch loader_agv]
```

#### AGVC 管理系統啟動流程
```mermaid
graph TD
    A[Docker Container Start] --> B[startup.agvc.bash]
    B --> C[SSH Service]
    B --> D[Zenoh Router]
    B --> E[Load Shared Workspaces]
    B --> F[PostgreSQL]
    B --> G[pgAdmin4]
    B --> H[Nginx]
    E --> I[Manual AGVC Services]
    I --> J[db_proxy_ws]
    I --> K[ecs_ws]
    I --> L[rcs_ws]
    I --> M[wcs_ws]
    I --> N[web_api_ws]
```

### 健康檢查和故障排除流程

#### 系統健康檢查指令
```bash
# 檢查容器狀態
docker compose -f <compose-file> ps

# 檢查基礎服務
docker compose -f <compose-file> exec <container> bash -c "
ps aux | grep -E '(ssh|zenoh|postgres)'
"

# 檢查 Zenoh Router 狀態
docker compose -f <compose-file> exec <container> bash -c "
cat /tmp/zenoh_router.pid && pgrep -f rmw_zenohd
"

# 檢查 ROS 2 環境
docker compose -f <compose-file> exec <container> bash -c "
printenv | grep -E '(ROS_DISTRO|RMW_IMPLEMENTATION)'
"
```

#### 常見故障排除流程
1. **容器啟動失敗**
   - 檢查 Docker 服務狀態
   - 查看容器日誌
   - 檢查端口衝突
   - 驗證映像完整性

2. **Zenoh Router 連線問題**
   - 檢查 port 7447 可用性
   - 驗證 routerconfig.json5 配置
   - 重啟 Zenoh Router 服務
   - 檢查網路連線

3. **資料庫連線問題** (AGVC 系統)
   - 檢查 PostgreSQL 服務狀態
   - 驗證資料庫憑證
   - 檢查連線池配置
   - 查看資料庫日誌

### 效能監控和日誌管理

#### 效能監控指標
```bash
# 容器資源使用情況
docker stats

# 系統資源監控
docker compose -f <compose-file> exec <container> bash -c "
top -bn1 | head -20
free -h
df -h
"

# 網路連線監控
docker compose -f <compose-file> exec <container> bash -c "
netstat -tulpn | grep -E '(2200|7447|5432|5050|80)'
"
```

#### 日誌管理策略
```bash
# 查看即時日誌
docker compose -f <compose-file> logs -f [service_name]

# 日誌輪轉配置
docker compose -f <compose-file> logs --tail=1000 [service_name]

# 系統日誌收集
docker compose -f <compose-file> exec <container> bash -c "
tail -f /tmp/zenoh_router.log
tail -f /tmp/agv.log
"
```

## 📚 與應用目錄指導文檔的關係

### 職責分工
- **根目錄 (.augment-guidelines)**: 專注於部署、管理、系統整體架構
- **應用目錄 (app/.augment-guidelines)**: 專注於開發、技術細節、ROS 2 實作

### 交叉引用機制
- 詳細的 ROS 2 開發指導 → 參考 `app/.augment-guidelines`
- 工作空間技術細節 → 參考 `app/README.md`
- 容器內開發工具 → 參考 `app/setup.bash` (統一環境腳本，自動檢測 AGV/AGVC)

### 避免內容重複原則
- 根目錄不重複 ROS 2 節點開發細節
- 根目錄不重複工作空間建置指令
- 根目錄不重複 Zenoh 技術配置細節
- 專注於從宿主機角度的系統管理

## 🔧 AI 助理使用指導

### ⚠️ 重要提醒：此文檔僅適用於宿主機環境
**如果您在容器內環境（`/app`），請立即切換到應用目錄的 `app/.augment-guidelines` 文檔**

### 環境識別檢查
AI 助理必須首先確認當前執行環境：

#### 🔍 環境檢查指令
```bash
# 檢查當前工作目錄
pwd
# 預期輸出：/home/ct/RosAGV

# 檢查是否可執行 Docker 指令
if command -v docker > /dev/null; then
    echo "✅ 在宿主機環境，可執行 Docker 指令"
else
    echo "❌ Docker 不可用"
fi

# 檢查是否可直接執行 ROS 2
if command -v ros2 > /dev/null; then
    echo "❌ 可直接執行 ros2，可能在容器內環境"
else
    echo "✅ 無法直接執行 ros2，確認在宿主機環境"
fi

# 檢查專案檔案結構
if [ -f "docker-compose.yml" ] && [ -f "Dockerfile" ]; then
    echo "✅ 確認在 RosAGV 根目錄"
else
    echo "❌ 不在正確的專案根目錄"
fi
```

### 指令選擇原則
**宿主機環境專用指令格式**:
```bash
# 啟動和管理容器
docker compose -f docker-compose.yml up -d
docker compose -f docker-compose.agvc.yml up -d

# 進入容器執行指令
docker compose -f docker-compose.yml exec rosagv bash -c "command"
docker compose -f docker-compose.agvc.yml exec agvc_server bash -c "command"

# 查看容器狀態和日誌
docker compose -f <compose-file> ps
docker compose -f <compose-file> logs -f [service_name]
```

**❌ 絕對不要在宿主機環境使用的指令**:
```bash
# 這些是容器內指令，不要在宿主機使用
ros2 launch loader_agv launch.py
colcon build
source setup.bash
build_all
start_ecs
```

### 文檔更新責任
AI 助理在協助程式碼修改時，應提醒開發者：
1. 檢查是否需要更新對應層級的 README.md
2. 驗證文檔中的指令和狀態描述
3. 確保跨環境的一致性

## 🎯 開發最佳實踐

### 容器化開發原則
- 所有開發工作在容器內進行
- 使用 volume 掛載進行程式碼同步
- 避免在容器內安裝額外套件
- 使用統一的基礎映像

### 跨環境通訊
- 使用 Zenoh RMW 進行 ROS 2 通訊
- 確保網路配置允許跨容器通訊
- 統一的命名空間和主題命名

### 安全和維護
- 定期更新基礎映像
- 監控容器資源使用
- 備份重要配置和資料
- 實施適當的存取控制

---

**詳細技術指導請參考**: `app/.augment-guidelines`  
**開發環境設定請參考**: `app/README.md`  
**工作空間技術細節請參考**: `app/*_ws/README.md`
