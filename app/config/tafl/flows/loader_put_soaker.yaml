metadata:
  id: "loader_put_soaker"
  name: "Loader AGV 放料到泡藥機"
  enabled: false
  version: "1.0.0"
  description: "檢查 Loader AGV 車上載具和泡藥機空位，創建放料任務（特殊設備：1格處理）"
  author: "TAFL System"
  created_at: "2025-10-16"
  tags: ["loader", "soaker", "put", "automation", "special-equipment"]

settings:
  execution_interval: 12  # Execute every 12 seconds

variables:
  priority: 4  # 修正：priority 必須在 1-10 範圍內
  model: "Loader"
  soaker_equipment_id: 204
  # 6個 Station 配置（特殊設備，全部1格處理）
  stations:
    - station: 1
      work_id: 2040102
      ports: [2041]
      batch_size: 1
      name: "泡藥機A"
    - station: 2
      work_id: 2040202
      ports: [2042]
      batch_size: 1
      name: "泡藥機B"
    - station: 3
      work_id: 2040302
      ports: [2043]
      batch_size: 1
      name: "泡藥機C"
    - station: 4
      work_id: 2040402
      ports: [2044]
      batch_size: 1
      name: "泡藥機D"
    - station: 5
      work_id: 2040502
      ports: [2045]
      batch_size: 1
      name: "泡藥機E"
    - station: 6
      work_id: 2040602
      ports: [2046]
      batch_size: 1
      name: "泡藥機F"

flow:
  # 調試：流程開始
  - notify:
      message: "🔍 開始執行 loader_put_soaker 流程"

  # 查詢所有房間
  - query:
      target: rooms
      where:
        enable: 1
      as: active_rooms
      description: "查詢所有啟用的房間"

  # 調試：房間數量
  - notify:
      message: "找到 ${active_rooms.length} 個啟用的房間"

  # 遍歷每個房間
  - for:
      in: "${active_rooms}"
      as: room
      do:
        # 調試：進入房間迴圈
        - notify:
            message: "📍 處理房間 ${room.id} (${room.name})"
        # 查詢 Loader AGV (AGV表無room_id欄位，使用model過濾)
        - query:
            target: agvs
            where:
              model: "Loader"
              enable: 1
            as: loader_agvs
            description: "查詢所有 Loader AGV"

        # 遍歷每個 Loader AGV
        - for:
            in: "${loader_agvs}"
            as: agv
            do:
              # 查詢 AGV Port 1、3 的泡藥中載具（前段流程端口）
              - query:
                  target: carriers
                  where:
                    port_in: [2101, 2103]  # Port 1、3（Carrier 沒有 agv_id，用 port_id 查詢）
                    status_id: 401  # 泡藥中
                    room_id: "${room.id}"  # 過濾當前房間的載具
                  as: port13_soaker_carriers
                  description: "查詢 AGV Port 1、3 的泡藥中載具（當前房間）"

              # 檢查泡藥中載具數量
              - set:
                  soaker_carrier_count: "${port13_soaker_carriers.length}"
                  required_count: 1  # 特殊設備：1格處理
                  has_enough_carriers: "${soaker_carrier_count >= required_count}"

              # 調試：記錄載具情況
              - notify:
                  message: "Room ${room.id}: AGV ${agv.name} port13_soaker_carriers=${soaker_carrier_count}, required=${required_count}, has_carriers=${has_enough_carriers}"

              # 如果 AGV 有載具，遍歷6個泡藥機 Station
              - if:
                  condition: "${has_enough_carriers}"
                  then:
                    # 遍歷6個 Station（泡藥機 A-F）
                    - for:
                        in: "${stations}"
                        as: station
                        do:
                          # 查詢該 Station 的空位（1個 Port）
                          - query:
                              target: eqp_ports
                              where:
                                equipment_id: "${soaker_equipment_id}"
                                port_in: "${station.ports}"
                                status: "empty"
                              as: empty_ports
                              description: "查詢泡藥機 Station ${station.station} 空位"

                          # 檢查空位（特殊設備：1格處理）
                          - set:
                              empty_count: "${empty_ports.length}"
                              required_space: 1
                              has_enough_space: "${empty_count >= required_space}"

                          # 如果軟體狀態顯示有空位，檢查實際感測器在席信號
                          - if:
                              condition: "${has_enough_space}"
                              then:
                                # 查詢這些 Port 的 Presence 信號
                                - query:
                                    target: eqp_signals
                                    where:
                                      eqp_port_id_in: "${station.ports}"
                                      name_like: "%Presence"
                                      value: "0"  # 0 = 無載具在席（空位）
                                    as: empty_presence_signals
                                    description: "確認感測器無載具在席"

                                # 檢查是否所有 Port 的 Presence 都是 0
                                - set:
                                    presence_check_count: "${empty_presence_signals.length}"
                                    presence_all_empty: "${presence_check_count >= required_space}"

                                # 如果軟體狀態和硬體感測器都確認有空位，檢查重複任務
                                - if:
                                    condition: "${has_enough_space && presence_all_empty}"
                                    then:
                                      # 檢查是否已存在未完成的放料任務
                                      - query:
                                          target: tasks
                                          where:
                                            work_id: "${station.work_id}"
                                            room_id: "${room.id}"
                                            status_id_in: [0, 1, 2, 3]  # 未完成的狀態
                                          as: existing_tasks
                                          description: "檢查是否已存在放料任務"

                                      # 如果沒有重複任務，創建新任務
                                      - if:
                                          condition: "${existing_tasks.length == 0}"
                                          then:
                                            # 創建 Loader AGV 放料任務
                                            - create:
                                                target: task
                                                with:
                                                  type: "loader_put"
                                                  name: "房間${room.id}泡藥機 Station${station.station} 放料"
                                                  description: "AGV Port 1、3 有${soaker_carrier_count}個泡藥中載具，${station.name}有空位，感測器確認空位（特殊設備：1格處理）"
                                                  work_id: "${station.work_id}"
                                                  room_id: "${room.id}"
                                                  priority: "${priority}"
                                                  status_id: 1  # PENDING
                                                  parameters:
                                                    station: "${station.station}"
                                                    work_id: "${station.work_id}"
                                                    room_id: "${room.id}"
                                                    equipment_id: "${soaker_equipment_id}"
                                                    ports: "${station.ports}"
                                                    batch_size: "${station.batch_size}"
                                                    model: "${model}"
                                                    source_agv_ports: [2101, 2103]  # 從 Port 1、3 取出
                                                    carrier_count: "${soaker_carrier_count}"
                                                    carrier_status: 401  # 泡藥中
                                                    empty_space_count: "${empty_count}"
                                                    presence_verified: true
                                                    reason: "AGV Port 1、3 有泡藥中載具，泡藥機有空位，感測器確認空位（雙重驗證）"
                                                description: "創建 Loader AGV 放料任務（特殊設備，含感測器驗證）"
