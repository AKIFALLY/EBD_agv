meta:
  system: linear_flow_v2
  version: "2.0.0"
  author: "Flow WCS System"
  created_at: "2025-08-19"
  description: "滿料架搬運到人工收料區流程 - 將房間出口的滿料架搬運到人工收料區"

flow:
  id: "full_rack_to_manual_area"
  name: "滿料架到人工收料區"
  work_id: "220002"  # 滿料架搬運專用 work_id
  enabled: true
  priority: 80  # 高優先級

workflow:
  - section: "檢查人工收料區空位"
    description: "檢查人工收料區 (51-55) 是否有空位可放置滿料架"
    steps:
      - id: "query_manual_area_locations"
        exec: "query.locations"
        params:
          type: "manual_receive_area"
          rooms: [51, 52, 53, 54, 55]  # 人工收料區位置
          has_rack: false  # 查詢沒有架台的位置（空位）
        store: "available_manual_locations"
        description: "查詢人工收料區空位"

      - id: "check_has_space"
        exec: "check.empty"
        params:
          data: "${available_manual_locations}"
        store: "no_space_available"
        description: "檢查是否有空位"

      - id: "stop_if_no_space"
        exec: "control.stop_flow"
        params:
          reason: "人工收料區已滿，沒有空位"
        skip_if: "!${no_space_available}"
        description: "如果沒有空位則停止流程"

      - id: "log_available_space"
        exec: "action.log_message"
        params:
          message: "📦 人工收料區有 ${available_manual_locations.length} 個空位可用"
          level: "info"
          tags: ["manual_area", "space_check"]
        skip_if: "${no_space_available}"
        description: "記錄可用空位數量"

  - section: "查詢房間出口滿料架"
    description: "查詢所有房間出口位置的滿料架"
    steps:
      - id: "query_outlet_locations"
        exec: "query.locations"
        params:
          type: "room_outlet"
          rooms: [1, 2, 3, 4, 5]  # 所有房間
          has_rack: true  # 有架台的位置
        store: "outlet_locations_with_rack"
        skip_if: "${no_space_available}"
        description: "查詢房間出口有架台的位置"

      - id: "check_has_outlet_racks"
        exec: "check.empty"
        params:
          data: "${outlet_locations_with_rack}"
        store: "no_outlet_racks"
        skip_if: "${no_space_available}"
        description: "檢查是否有出口架台"

      - id: "stop_if_no_outlet_racks"
        exec: "control.stop_flow"
        params:
          reason: "房間出口沒有架台需要搬運"
        skip_if: "!${no_outlet_racks} || ${no_space_available}"
        description: "如果沒有出口架台則停止流程"

  - section: "檢查每個出口的滿料架"
    description: "遍歷每個出口位置，檢查是否有滿料架需要搬運"
    steps:
      - id: "foreach_outlet_locations"
        exec: "foreach"
        params:
          items: "${outlet_locations_with_rack}"
          var: "location"
          steps:
            - id: "query_full_racks_at_location"
              exec: "query.racks"
              params:
                location_id: "${location.id}"
                status: "available"  # 可用狀態的架台
              store: "racks_at_location"
              description: "查詢該位置的架台"

            - id: "check_has_racks"
              exec: "check.empty"
              params:
                data: "${racks_at_location}"
              store: "no_racks"
              description: "檢查是否有架台"

            - id: "check_rack_full"
              exec: "check.rack_status"
              params:
                rack_id: "${racks_at_location[0].id}"
                side: "both"  # 檢查兩面
                check_type: "full"  # 檢查是否滿載
              store: "rack_is_full"
              skip_if: "${no_racks}"
              description: "檢查架台是否滿載"

            - id: "log_full_rack_found"
              exec: "action.log_message"
              params:
                message: "🎯 發現滿料架 - 位置: ${location.name} (房間 ${location.room_id}), 架台: ${racks_at_location[0].rack_id}"
                level: "info"
                tags: ["full_rack", "outlet", "room_${location.room_id}"]
              skip_if: "${no_racks} || !${rack_is_full}"
              description: "記錄發現滿料架"

            - id: "get_target_location"
              exec: "control.update_variable"
              params:
                variable: "target_location_id"
                operation: "set"
                value: "${available_manual_locations[0].id}"
              skip_if: "${no_racks} || !${rack_is_full}"
              description: "獲取目標人工收料區位置"

            - id: "check_existing_transport_task"
              exec: "check.task_exists"
              params:
                type: "FULL_RACK_TRANSPORT"
                rack_id: "${racks_at_location[0].id}"
                status: ["pending", "in_progress"]
              store: "transport_task_exists"
              skip_if: "${no_racks} || !${rack_is_full}"
              description: "檢查是否已存在未完成的搬運任務"

            - id: "create_transport_task"
              exec: "task.create_task"
              params:
                type: "FULL_RACK_TRANSPORT"
                work_id: "220002"  # 滿料架搬運 work_id
                source_location_id: "${location.id}"
                target_location_id: "${target_location_id}"
                rack_id: "${racks_at_location[0].id}"
                room_id: "${location.room_id}"
                priority: "high"  # 高優先級
                metadata:
                  rack_name: "${racks_at_location[0].rack_id}"
                  source_location: "${location.name}"
                  target_location: "人工收料區-${target_location_id}"
                  room_id: "${location.room_id}"
                  transport_reason: "滿料架需要人工處理"
                  model: "Cargo"  # Cargo AGV 負責搬運
                  # 路徑規劃：從出口到人工收料區
                  nodes: ["${location.node_id}", "${available_manual_locations[0].node_id}"]
              store: "transport_task_id"
              skip_if: "${no_racks} || !${rack_is_full} || ${transport_task_exists}"
              description: "創建滿料架搬運任務"

            - id: "log_task_created"
              exec: "action.log_message"
              params:
                message: "✅ 創建搬運任務 - 架台: ${racks_at_location[0].rack_id}, 從 ${location.name} 到 人工收料區-${target_location_id}, 任務ID: ${transport_task_id}"
                level: "info"
                tags: ["task_created", "transport", "manual_area"]
              skip_if: "${no_racks} || !${rack_is_full} || ${transport_task_exists}"
              description: "記錄任務創建"

            - id: "update_available_locations"
              exec: "control.update_variable"
              params:
                variable: "available_manual_locations"
                operation: "set"
                value: "${available_manual_locations.slice(1)}"  # 移除第一個已使用的位置
              skip_if: "${no_racks} || !${rack_is_full} || ${transport_task_exists}"
              description: "更新可用位置列表"

            - id: "check_remaining_space"
              exec: "check.empty"
              params:
                data: "${available_manual_locations}"
              store: "no_more_space"
              skip_if: "${no_racks} || !${rack_is_full}"
              description: "檢查是否還有剩餘空位"

            - id: "log_space_full"
              exec: "action.log_message"
              params:
                message: "⚠️ 人工收料區已滿，停止創建更多任務"
                level: "warning"
                tags: ["manual_area_full", "space_limit"]
              skip_if: "${no_racks} || !${rack_is_full} || !${no_more_space}"
              description: "記錄空間已滿"

        skip_if: "${no_space_available} || ${no_outlet_racks}"
        description: "遍歷所有出口位置檢查滿料架"

  - section: "監控人工收料區狀態"
    description: "檢查人工收料區的使用情況並發送通知"
    steps:
      - id: "query_manual_area_racks"
        exec: "query.racks"
        params:
          location_id: [51, 52, 53, 54, 55]  # 人工收料區所有位置
          status: "available"
        store: "manual_area_racks"
        description: "查詢人工收料區的架台數量"

      - id: "count_manual_racks"
        exec: "control.count_items"
        params:
          variable: "${manual_area_racks}"
        store: "manual_rack_count"
        description: "計算人工收料區架台數量"

      - id: "calculate_usage_rate"
        exec: "control.update_variable"
        params:
          variable: "usage_rate"
          operation: "set"
          value: "${manual_rack_count * 100 / 5}"  # 計算使用率百分比
        description: "計算使用率"

      - id: "log_manual_area_status"
        exec: "action.log_message"
        params:
          message: "📊 人工收料區狀態 - 使用率: ${usage_rate}% (${manual_rack_count}/5)"
          level: "info"
          tags: ["manual_area_status", "monitoring"]
        description: "記錄人工收料區狀態"

      - id: "check_high_usage"
        exec: "control.switch"
        params:
          value: "${manual_rack_count}"
          cases:
            "4": true
            "5": true
          default: false
        store: "high_usage"
        description: "檢查是否高使用率"

      - id: "send_high_usage_notification"
        exec: "action.send_notification"
        params:
          message: "⚠️ 人工收料區使用率高: ${usage_rate}% (${manual_rack_count}/5)，請盡快處理滿料架"
          level: "warning"
          priority: "high"
        skip_if: "!${high_usage}"
        description: "發送高使用率通知"

  - section: "總結"
    description: "流程執行總結"
    steps:
      - id: "count_processed_locations"
        exec: "control.count_items"
        params:
          variable: "${outlet_locations_with_rack}"
        store: "total_outlets"
        description: "計算處理的出口位置數量"

      - id: "log_completion"
        exec: "action.log_message"
        params:
          message: "🏁 滿料架搬運流程完成 - 檢查了 ${total_outlets} 個出口位置，人工收料區使用率: ${usage_rate}%"
          level: "info"
          tags: ["completion", "full_rack_transport", "summary"]
        description: "記錄完成訊息"

settings:
  log_level: "INFO"
  enable_checkpoint: true
  parallel_execution: false
  timeout: 180  # 3分鐘超時
  retry_on_failure: true
  max_retries: 3
  # 執行頻率控制 - 每3分鐘執行一次
  execution_interval: 180  # seconds
  last_execution: null
  # 滿料架搬運特定設定
  transport_settings:
    max_concurrent_tasks: 3  # 最多同時3個搬運任務
    priority_threshold: 80  # 優先級閾值
    manual_area_capacity: 5  # 人工收料區容量