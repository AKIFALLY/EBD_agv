# RosAGV 應用目錄 AI 開發指導文檔

## ⚠️ 重要：執行環境識別
**執行環境**: Docker 容器內部 (`/app` 目錄)
**目標用戶**: ROS 2 開發者、AI 助理
**最後更新**: 2025-01-17

### 🔍 當前環境確認
**此文檔適用於已進入 Docker 容器後的開發工作**

**環境識別特徵**:
- 當前工作目錄：`/app`
- 可直接執行：`ros2`、`colcon`、`source setup.bash`
- 環境變數：`RMW_IMPLEMENTATION=rmw_zenoh_cpp`
- 容器名稱：`rosagv` 或 `agvc_server`

**指令執行方式**:
```bash
# ✅ 正確：容器內直接執行
ros2 launch loader_agv launch.py
colcon build
source setup.bash
build_all

# ❌ 錯誤：不要在容器內使用 docker compose
docker compose exec rosagv bash  # 這是宿主機指令
```

**如果您在宿主機環境**，請參考根目錄的 `/app/host/.augment-guidelines` 文檔。

## 🏗️ 雙環境架構開發指導

### 🚗 AGV 車載系統開發原則
**容器名稱**: `rosagv`
**啟動腳本**: `startup.agv.bash`
**Docker Compose**: `docker-compose.yml`

**開發環境特點**:
- 專注於車載控制邏輯和即時響應
- 直接與 PLC 設備和感測器通訊
- 使用狀態機模式管理 AGV 行為
- 支援手動控制（搖桿）和自動導航

**實際啟動的工作空間** (✅ 自動載入):
- `keyence_plc_ws` - Keyence PLC 通訊
- `plc_proxy_ws` - PLC 代理服務
- `agv_cmd_service_ws` - AGV 命令服務
- `joystick_ws` - 搖桿控制
- `agv_ws` - AGV 核心控制 (啟動 loader_agv launch)
- `path_algorithm` - 路徑規劃演算法

**未啟動的工作空間** (❌ 需手動啟動):
- `sensorpart_ws` - 感測器整合系統

### 🖥️ AGVC 管理系統開發原則
**容器名稱**: `agvc_server`
**啟動腳本**: `startup.agvc.bash`
**Docker Compose**: `docker-compose.agvc.yml`

**開發環境特點**:
- 專注於車隊管理和系統整合
- 整合資料庫、Web 介面、外部系統
- 支援多車協調和任務調度
- 提供管理介面和監控功能

**共用工作空間** (✅ 自動載入):
- `keyence_plc_ws`, `plc_proxy_ws`, `agv_cmd_service_ws`, `joystick_ws`, `agv_ws`, `path_algorithm`

**AGVC 專用工作空間** (❌ 未在啟動腳本中，需手動啟動):
- `db_proxy_ws` - 資料庫代理服務
- `ecs_ws` - 設備控制系統 (⚠️ 依賴已載入: plc_proxy_ws, keyence_plc_ws)
- `rcs_ws` - 車隊控制系統
- `wcs_ws` - 倉庫控制系統
- `web_api_ws` - Web API 和前端介面
- `kuka_fleet_ws` - KUKA 車隊整合

**重要依賴關係**:
```
ecs_ws → plc_proxy_ws (✅ 已載入)
ecs_ws → keyence_plc_ws (✅ 已載入)
ecs_ws → db_proxy_ws (❌ 未載入)
```

## 🛠️ 技術棧最佳實踐

### Python 執行環境
**重要**: 在 Ubuntu 24.04 環境下，必須使用 `python3` 而非 `python`

**正確的 Python 指令**:
```bash
# ✅ 正確：使用 python3
python3 script.py
python3 -m pip install package
python3 -c "import sys; print(sys.version)"

# ❌ 錯誤：不要使用 python
python script.py          # 可能不存在或指向錯誤版本
pip install package       # 使用 pip3 更明確
```

**套件管理**:
```bash
# 安裝 Python 套件
pip3 install package_name
pip3 install -r requirements.txt

# 檢查已安裝套件
pip3 list
pip3 show package_name

# 虛擬環境 (如果需要)
python3 -m venv venv_name
source venv_name/bin/activate
```

**在 ROS 2 環境中**:
```bash
# ROS 2 Python 節點執行
ros2 run package_name node_name
python3 /app/package_ws/src/package/script.py

# 測試 Python 模組
python3 -m pytest test_file.py
python3 -m unittest discover
```

### Python 虛擬環境配置
**Docker 容器內虛擬環境配置**:
- **虛擬環境路徑**: `/opt/pyvenv_env`
- **PYTHONPATH 自動設定**: `/opt/pyvenv_env/lib/python3.12/site-packages`
- **自動載入機制**: 透過容器啟動腳本自動配置

**虛擬環境中安裝的套件**:
```bash
# Web 開發相關套件
fastapi, uvicorn, pydantic          # FastAPI Web 框架
jinja2, python-multipart            # 模板和檔案上傳
python-socketio                     # WebSocket 支援

# 資料庫相關套件
sqlalchemy, sqlmodel, psycopg2      # PostgreSQL 資料庫操作

# 其他工具套件
pygame                              # 遊戲開發和圖形處理
paho-mqtt                           # MQTT 通訊
requests                            # HTTP 請求
networkx                            # 圖形和網路分析
```

**正確的虛擬環境套件管理**:
```bash
# ✅ 正確：使用虛擬環境的 pip
/opt/pyvenv_env/bin/pip3 install package_name
/opt/pyvenv_env/bin/pip3 list
/opt/pyvenv_env/bin/pip3 show package_name

# ❌ 錯誤：可能安裝到系統環境
pip3 install package_name
python3 -m pip install package_name
```

**環境驗證和診斷**:
```bash
# 檢查 PYTHONPATH 配置
echo $PYTHONPATH

# 確認 Python 模組搜尋路徑
python3 -c "import sys; print('\n'.join(sys.path))"

# 檢查特定套件安裝位置
python3 -c "import fastapi; print(fastapi.__file__)"
python3 -c "import sqlalchemy; print(sqlalchemy.__file__)"

# 列出虛擬環境套件
/opt/pyvenv_env/bin/pip3 list

# 檢查虛擬環境是否啟用
echo $VIRTUAL_ENV
```

**使用場景**:
- **Web API 工作空間** (`web_api_ws`): 使用 FastAPI、SQLAlchemy 等
- **資料庫代理工作空間** (`db_proxy_ws`): 使用 SQLAlchemy、psycopg2 等
- **一般 ROS 2 工作空間**: 通常不需要虛擬環境套件，使用系統套件即可

**db_proxy_ws 腳本工具驗證** (2025-07-17 完整測試):
- ✅ **腳本工具**: 4 個管理腳本，100% 通過率，支援宿主機和容器環境
- ✅ **RMW 相容性**: 完整支援 rmw_zenoh_cpp 和 rmw_fastrtps_cpp
- ✅ **智能降級**: 自動檢測環境並選擇最佳測試方式
- ✅ **效能驗證**: 初始化 ~1 秒，連線測試 ~3-5 秒，狀態檢查 ~2-3 秒
- ✅ **故障排除**: 完整的診斷工具和解決方案

### ROS 2 Jazzy + rmw_zenoh_cpp 配置
**環境變數設定**:
```bash
export ROS_DISTRO="jazzy"
export RMW_IMPLEMENTATION="rmw_zenoh_cpp"
export ZENOH_ROUTER_CONFIG_URI="/app/routerconfig.json5"
```

**Zenoh Router 管理** (在 setup.bash 中定義):
```bash
# 啟動 Zenoh Router
manage_zenoh start

# 檢查狀態
manage_zenoh status
check_zenoh_status  # 或使用別名: zenoh

# 重啟 Zenoh Router
manage_zenoh restart

# 停止 Zenoh Router
manage_zenoh stop
```

**驗證方法**:
```bash
# 檢查環境變數
printenv | grep -E "(ROS_DISTRO|RMW_IMPLEMENTATION|ZENOH_ROUTER_CONFIG_URI)"

# 檢查 Zenoh Router 狀態
pgrep -f rmw_zenohd && echo "✅ Zenoh Router 運行中"

# 檢查 Zenoh 端口
netstat -tuln | grep ':7447 ' && echo "✅ 端口 7447 已開啟"

# 測試 ROS 2 節點通訊
timeout 5 ros2 node list && echo "✅ ROS 2 通訊正常"
```

**RMW 實作對比** (基於 db_proxy_ws 測試驗證):
- **rmw_zenoh_cpp**: 預設推薦，提供詳細連線池日誌，需要 Zenoh Router
- **rmw_fastrtps_cpp**: 標準實作，無需額外服務，適合簡單測試
- **效能**: 兩者效能相當，功能完全一致
- **穩定性**: 都經過完整測試驗證，100% 通過率

**重要注意事項**:
1. **Zenoh Router 依賴**: 使用 rmw_zenoh_cpp 時必須確保 Zenoh Router 運行
2. **環境載入順序**: 必須先載入 `/opt/ws_rmw_zenoh/install/setup.bash`
3. **容器啟動**: Zenoh Router 在容器啟動時自動啟動
4. **故障排除**: 如果 ROS 2 通訊異常，首先檢查 Zenoh Router 狀態

### Docker Compose V2 語法和容器內開發流程
**正確的指令語法**:
```bash
# 從宿主機進入容器
docker compose -f docker-compose.yml exec rosagv bash
docker compose -f docker-compose.agvc.yml exec agvc_server bash

# 容器內開發流程
source setup.bash                    # 載入開發環境
build_all                           # 建置所有工作空間
test_all                            # 測試所有工作空間
check_system_status                 # 檢查系統狀態
```

### 工作空間依賴關係管理
**依賴檢查原則**:
1. 檢查 `package.xml` 中的 `<exec_depend>` 聲明
2. 檢查 Python import 語句
3. 檢查 ROS 2 服務和主題通訊
4. 確認啟動腳本中的載入順序

**ECS 依賴範例**:
```python
# ecs_core.py 中的直接依賴
from plc_proxy.plc_client import PlcClient
from keyence_plc.keyence_plc_memory import PlcMemory

# 驗證依賴可用性
python3 -c "from plc_proxy.plc_client import PlcClient; from keyence_plc.keyence_plc_memory import PlcMemory; print('✅ ECS 依賴可用')"
```

## 🔧 開發工具使用指南

### setup.bash 統一環境腳本使用
**環境自動檢測**：`setup.bash` 會自動檢測當前是 AGV 還是 AGVC 環境，並提供對應功能
**環境載入**:
```bash
all_source                          # 載入所有工作空間
source setup.bash                   # 載入開發環境和函數
```

**建置和測試**:
```bash
build_all                           # 建置所有工作空間
build_ws <workspace_name>           # 建置指定工作空間
test_all                            # 測試所有工作空間
test_ws <workspace_name>            # 測試指定工作空間
clean_all                           # 清理所有工作空間
clean_ws <workspace_name>           # 清理指定工作空間
```

**狀態檢查**:
```bash
check_system_status                 # 完整系統狀態檢查
check_ros_env                       # ROS 2 環境檢查
check_zenoh_status                  # Zenoh Router 狀態檢查
```

**便捷別名**:
```bash
ba                                  # build_all
ta                                  # test_all
ca                                  # clean_all
sa                                  # all_source
status                              # check_system_status
zenoh                               # check_zenoh_status
rosenv                              # check_ros_env
```

## 通用開發原則
- 回覆請使用繁體中文。
- 所有程式碼應符合 Python 3.12+ 語法（容器內版本）。
- 所有程式碼建議包含必要的 import 區塊，便於直接複製使用。
- 盡量以 async 為主設計 API（除非明確需求）。
- 優先考慮可讀性與可維護性，適當抽象重構為模組。
- 使用 colcon build 建置 ROS 2 工作空間，並使用 `source install/setup.bash` 設定環境。

## 專案架構原則
- 每個工作空間應包含完整的 README.md 說明文檔。
- 使用模組化設計，將功能分離到不同的套件中。
- 配置文件統一放置在 `/app/config/` 目錄下。
- 日誌文件統一放置在各工作空間的 `log/` 目錄下。
- 測試文件放置在 `tests/` 目錄下。

## FastAPI
- 使用 `@router.get`, `@router.post` 等路由裝飾器設計 API。
- API 定義請使用 Pydantic（或 SQLModel）作為 request/response schema。
- 優先使用 FastAPI 的依賴注入（Depends）管理 DB session。
- 回傳格式建議為 JSONResponse，結構為：`{"success": true/false, "data": ..., "message": ""}`。
- 支援 WebSocket 即時通訊，用於狀態監控和即時資料推送。
- 使用 uvicorn 作為 ASGI 伺服器，支援 `--reload` 開發模式。

## Jinja2
- 用於簡單的 Web UI 模板渲染。
- 使用 `templates/` 資料夾儲存 HTML 模板。
- 模板中變數須明確，以易懂名稱命名。
- 與 FastAPI 的 `Jinja2Templates` 整合時，請示範如何載入與 render。
- 支援 Flask 應用的模板渲染，靜態資源放置在 `static/` 目錄。

## SQLModel
- 所有資料表 model 均繼承 `SQLModel` 或 `SQLModel, table=True`。
- 優先使用 type hint 和 `Field(default=...)` 描述欄位。
- 使用 `Session` 的 context manager (`with Session(...) as session:`)。
- CRUD 操作應封裝於 `crud/` 模組中，方便管理與單元測試。
- 使用連線池管理資料庫連線，配置 pool_size、max_overflow 等參數。
- 支援 PostgreSQL 作為主要資料庫，連線字串格式：`postgresql+psycopg2://user:pass@host/db`。

## ROS 2 Jazzy 開發規範
- 使用 `rclpy` 開發 ROS 2 Node，節點應模組化，主類別繼承自 `rclpy.node.Node`。
- 建議以 asyncio 或定時 callback 撰寫非同步處理邏輯。
- 與資料庫互動應以非同步設計並透過 ROS topic / service 進行橋接。
- 如果有 service 請包含 srv 定義與 call 範例。
- 使用 MultiThreadedExecutor 提升並發處理能力。
- 支援命名空間配置，適用於多 AGV 環境。
- 所有服務調用都應有超時機制，避免程式卡死。
- 完整的異常處理和日誌記錄。

## AGV 系統架構規範
### 狀態機設計
- 使用狀態機模式管理複雜的業務邏輯，清晰的狀態轉換和事件處理。
- 基礎狀態：Idle State（空閒）、Manual State（手動）、Auto State（自動）、Error State（錯誤）。
- 新增狀態應繼承 `BaseState` 或 `BaseRobotState` 類，實現 `enter()`, `handle()`, `leave()` 方法。

### PLC 通訊規範
- 透過 `plc_proxy` 套件與 PLC 進行通訊，支援 MR (Memory Relay) 和 DM (Data Memory) 操作。
- PLC 讀取週期建議為 50-100ms，避免過載。
- 支援 Modbus、Ethernet/IP、Keyence 等多種協定。
- 完整的錯誤處理和自動重連機制。

### 資料庫代理規範
- 使用 `db_proxy` 提供統一的資料庫操作介面。
- 支援任務管理、載具追蹤、貨架狀態、設備監控、交通區域管理。
- 使用連線池管理，支援高並發存取。
- 提供 SQL 查詢、通用 CRUD、批次操作等服務。

### AGVC 專用函數使用（統一在 setup.bash 中）
**環境自動檢測**：`setup.bash` 會自動檢測當前是 AGV 還是 AGVC 環境，並提供對應功能

**AGVC 環境載入**:
```bash
source setup.bash                   # 載入統一開發環境（自動檢測環境）
agvc_source                         # 載入 AGVC 專用工作空間（僅 AGVC 環境可用）
```

**資料庫管理**（僅 AGVC 環境）:
```bash
start_db                            # 啟動 PostgreSQL 資料庫
stop_db                             # 停止資料庫服務
```

**服務管理**（僅 AGVC 環境）:
```bash
start_ecs                           # 手動啟動 ECS 設備控制系統
```

**狀態檢查**（僅 AGVC 環境）:
```bash
check_agvc_status                   # 檢查 AGVC 系統狀態
```

**環境檢測函數**:
```bash
is_agvc_environment                 # 檢查當前是否為 AGVC 環境
# 返回 0 (AGVC 環境) 或 1 (AGV 環境)
```

### setup.augment.bash AI 助理專用函數使用
**AI 助理指令**:
```bash
source setup.augment.bash          # 載入 AI 助理環境
augment_help                        # 顯示所有可用指令
augment_system_status              # 生成完整系統狀態報告
augment_build_workspace <ws>       # 安全建置指定工作空間
augment_diagnose_issue <comp>      # 診斷指定組件問題
augment_dev_report                 # 生成開發環境報告
```

**錯誤代碼**:
- 0: 成功
- 1: 一般錯誤
- 2: 參數錯誤
- 3: 依賴缺失
- 4: 權限錯誤
- 5: 服務未運行

## 專案特定開發指導原則

### AGV 核心系統 (agv_ws) - ✅ 實際啟動
**啟動狀態**: 容器啟動時自動載入，執行 `ros2 launch loader_agv launch.py`
**功能**: AGV 節點基礎類提供 PLC 通訊、狀態管理、定時器等核心功能
**支援車型**: Loader、Cargo、Unloader 三種車型
**技術細節**:
- 狀態發布週期為 1.5 秒，PLC 記憶體大小為 65536 words (131072 bytes)
- 使用 agv_id 作為 ROS 2 命名空間，room_id 從 agv_id 後兩位數字提取
- 採用三層狀態架構：Base 層、AGV 層、Robot 層

### AGV 命令服務 (agv_cmd_service_ws) - ✅ 實際啟動
**啟動狀態**: 容器啟動時自動載入
**功能**: 提供手動控制和一般命令服務，透過 ROS 2 服務介面與 PLC 通訊
**命令類型**:
- 手動命令：前進、後退、左轉、右轉、左移、右移、煞車、啟用
- 一般命令：自動模式、緊急停止、系統重置、任務派發、任務取消、交通控制
**技術細節**: 所有命令都有對應的 PLC 位址配置，支援高頻率指令發送

### 資料庫代理系統 (db_proxy_ws) - ❌ 未在啟動腳本中
**啟動狀態**: 需要手動啟動或加入 AGVC 啟動腳本
**功能**: 提供完整的資料庫操作介面，包括任務管理、載具追蹤、貨架狀態等
**核心資料表**: Task、Work、Rack、Carrier、AGV、Location、Eqp、TrafficZone
**狀態管理表**: TaskStatus、RackStatus、CarrierStatus、LocationStatus
**連線池參數**: pool_size=10, max_overflow=20, pool_timeout=30, pool_recycle=3600
**手動啟動**: 需要先建置工作空間，然後啟動相關服務

### ECS 設備控制系統 (ecs_ws) - ⚠️ 依賴已載入
**啟動狀態**: 未在啟動腳本中，但依賴的 plc_proxy_ws 和 keyence_plc_ws 已載入
**功能**: 提供設備控制系統功能，包括門控制、設備信號管理、PLC 即時通訊
**依賴關係**:
- ✅ plc_proxy_ws (已載入)
- ✅ keyence_plc_ws (已載入)
- ❌ db_proxy_ws (未載入)
**技術細節**:
- 支援 MQTT 通訊協定，實現遠端設備控制和狀態監控
- PLC 通訊範圍：DM 7600-7620（設備控制）、DM 5000-5200（設備狀態）
- 讀取頻率為 100ms，支援同步/非同步門控制
**手動啟動**: `start_ecs` (在 setup.bash 中，僅 AGVC 環境可用)

### 搖桿控制系統 (joystick_ws) - ✅ 實際啟動
**啟動狀態**: 容器啟動時自動載入
**功能**: 提供 AGV 手動控制功能，支援 USB 搖桿或遊戲手把
**支援設備**: Xbox、PlayStation 控制器和通用 USB 搖桿
**安全機制**: 死人開關、速度限制、超時保護、狀態指示
**技術參數**: 發布頻率 50Hz，死區設定 0.1，設備超時 1.0 秒

### PLC 代理系統 (plc_proxy_ws) - ✅ 實際啟動
**啟動狀態**: 容器啟動時自動載入，被 ecs_ws 依賴
**功能**: 提供與 PLC 通訊的代理服務，支援多種 PLC 通訊協定
**服務介面**: ForceOn/Off、ReadData/WriteData、ReadContinuous/WriteContinuous
**操作類型**: 支援位元操作（MR）和資料操作（DM），批次讀寫多個記憶體位址
**效能參數**: max_concurrent_requests=10, retry_count=3, retry_delay=0.1

### RCS 車隊控制系統 (rcs_ws) - ❌ 未在啟動腳本中
**啟動狀態**: 需要手動啟動或加入 AGVC 啟動腳本
**功能**: 基於房間和車型的智能任務分派機制
**支援車型**: Cargo、Loader、Unloader
**分派邏輯**: 房內任務使用 `Loader{房間編號:02d}`、`Unloader{房間編號:02d}`，房外任務使用 `Cargo02`
**特點**: 完整的狀態管理、異常處理和日誌記錄

### WCS 倉庫控制系統 (wcs_ws) - ❌ 未在啟動腳本中
**啟動狀態**: 需要手動啟動或加入 AGVC 啟動腳本
**功能**: 提供倉庫管理和控制功能，整合 KUKA AGV 車隊管理
**整合對象**: KUKA Fleet 系統
**支援車型**: KUKA400i

### Web API 系統 (web_api_ws) - ❌ 未在啟動腳本中
**啟動狀態**: 需要手動啟動或加入 AGVC 啟動腳本
**功能**: 提供完整的 Web 介面和 API 服務，包含 FastAPI 後端和前端 UI
**API 端點**: AGV 管理、任務管理、設備監控、即時資料推送
**前端介面**: AGVCUI（管理介面）、OPUI（操作員介面）
**整合服務**: ROS 2、資料庫、MQTT、HTTP API、WebSocket

### 感測器系統 (sensorpart_ws) - ❌ 未在啟動腳本中
**啟動狀態**: 需要手動啟動
**功能**: 使用 TCP 通訊取得相機回傳的定位及 OCR 結果
**通訊協定**: TCP Socket
**資料類型**: 定位資訊、OCR 識別結果

### Keyence PLC 系統 (keyence_plc_ws) - ✅ 實際啟動
**啟動狀態**: 容器啟動時自動載入，被 ecs_ws 依賴
**功能**: 提供與 Keyence PLC 設備的專用通訊功能
**記憶體管理**: 高效的記憶體位址映射和資料快取機制
**通訊特點**: 非同步通訊，支援並發處理和回調機制
**最佳化**: 針對 Keyence 協定最佳化，支援多個 PLC 版本

### KUKA 車隊系統 (kuka_fleet_ws) - ❌ 未在啟動腳本中
**啟動狀態**: 需要手動啟動或加入 AGVC 啟動腳本
**功能**: 提供與 KUKA AGV 車隊系統的整合功能
**車隊管理**: 多車協調、任務派發、衝突避免、效能最佳化
**API 整合**: 與 KUKA 車隊 API 的完整整合和狀態同步
**資料同步**: 即時監控、任務追蹤、錯誤處理、日誌記錄

### 啟動管理系統 (launch_ws) - ❌ 未在啟動腳本中
**啟動狀態**: 有 ecs_launch 但未自動啟動
**功能**: 提供 RosAGV 系統的統一啟動和配置管理功能
**系統啟動**: 統一啟動、模組化啟動、參數配置、依賴管理
**配置管理**: 環境配置、參數模板、動態配置、配置驗證
**部署支援**: 容器化部署、分散式部署、自動化部署、監控整合

### 路徑規劃演算法 (path_algorithm) - ✅ 實際啟動
**啟動狀態**: 容器啟動時自動載入
**功能**: 提供 A* 演算法路徑規劃功能
**演算法**: A* 最佳路徑計算
**應用**: AGV 導航和路徑最佳化

## 命名慣例
- API 路由以小寫並使用 `-`（kebab-case）命名，如 `/update-task`。
- 資料表與欄位名稱使用 snake_case。
- 函式名稱以動詞開頭（如 `get_tasks`, `update_status`）。
- ROS Topic 名稱使用小寫與底線，如 `/agv/state_update`。
- ROS 2 套件名稱使用小寫與底線，如 `agv_base`, `plc_proxy`。

## 📚 強制文檔同步規則和驗證機制

### 文檔同步責任
**強制要求**: 任何程式碼修改完成後，開發者必須檢查並更新相關的 README.md 文檔

**觸發文檔更新的情況**:
- 新增或刪除工作空間
- 修改啟動腳本 (startup.agv.bash, startup.agvc.bash)
- 變更工作空間依賴關係
- 調整服務配置或啟動順序
- 新增或修改 API 介面
- 變更 Docker 配置

### 分層文檔更新指南

#### 工作空間級別 (`*_ws/README.md`)
**更新時機**: 修改工作空間內的程式碼、配置或功能
**更新內容**:
- 功能說明和使用方法
- 依賴關係聲明
- API 介面文檔
- 配置參數說明
- 故障排除指南

#### 應用目錄級別 (`/app/README.md`)
**更新時機**: 工作空間狀態變更、技術架構調整
**更新內容**:
- 工作空間狀態表格 (✅ 實際啟動 vs ❌ 未啟動 vs ⚠️ 依賴已載入)
- 技術架構圖和依賴關係
- 開發指南和工具使用說明
- Zenoh 配置和驗證方法

#### 根目錄級別 (`/app/host/README.md`)
**更新時機**: 部署方式變更、系統架構調整
**更新內容**:
- 部署指令和容器管理
- 系統架構圖
- 故障排除指南
- 快速開始指南

### 文檔更新檢查清單

#### ✅ 工作空間狀態檢查
- [ ] 工作空間啟動狀態標記正確 (✅/❌/⚠️)
- [ ] 依賴關係圖反映實際程式碼依賴
- [ ] 啟動腳本內容與文檔描述一致

#### ✅ Docker 指令檢查
- [ ] 使用正確的容器名稱 (rosagv, agvc_server)
- [ ] 使用正確的 compose 檔案 (docker-compose.yml, docker-compose.agvc.yml)
- [ ] Docker Compose V2 語法 (`docker compose` 而非 `docker-compose`)

#### ✅ 技術細節檢查
- [ ] ROS 2 + Zenoh 配置說明準確
- [ ] 環境變數和路徑正確
- [ ] 故障排除指令經過實際驗證

### 驗證步驟

#### 自動化驗證腳本建議
```bash
#!/bin/bash
# 文檔一致性檢查腳本

echo "🔍 檢查文檔一致性..."

# 檢查工作空間狀態標記
echo "檢查工作空間狀態標記..."
grep -r "✅\|❌\|⚠️" app/README.md

# 檢查 Docker 指令語法
echo "檢查 Docker 指令語法..."
if grep -r "docker-compose" /app/host/README.md app/README.md; then
    echo "❌ 發現舊版 docker-compose 語法，應使用 docker compose"
fi

# 檢查容器名稱一致性
echo "檢查容器名稱一致性..."
grep -r "rosagv\|agvc_server" /app/host/README.md app/README.md

# 驗證啟動腳本與文檔一致性
echo "驗證啟動腳本與文檔一致性..."
# 比較 startup.agv.bash 中的工作空間與文檔描述
```

#### 手動驗證檢查點
1. **啟動狀態驗證**: 對照啟動腳本檢查文檔中的工作空間狀態標記
2. **依賴關係驗證**: 檢查 package.xml 和 Python import 與文檔描述是否一致
3. **指令驗證**: 在對應環境中測試文檔中的指令是否可執行
4. **路徑驗證**: 確認檔案路徑、容器名稱、環境變數正確

## 測試與部署規範
- 測試前要引入 ROS2 環境時使用 `all_source` 指令。
- 使用 `colcon build` 建置專案，`colcon test` 執行測試。
- 測試檔案放在 `test/` 或 `tests/` 目錄，使用 `test_` 前綴命名。
- 所有測試必須在對應的容器環境中執行。
- 測試前必須確保 Zenoh Router 正常運行。

## 安全與維護規範
- 實施適當的網路安全措施和存取控制。
- 定期監控系統效能、連線狀態和通訊品質。
- 建立完整的資料備份和恢復機制。
- 保持框架和庫的最新版本，定期檢查安全漏洞。
- 完善的日誌記錄和錯誤追蹤機制。

## 程式碼範例規範
- 所有程式碼範例應包含完整的 import 區塊。
- 提供詳細的註解說明關鍵步驟與邏輯。
- 包含適當的錯誤處理和異常管理。
- 若範例過長，可分段說明，每段包含標題。
- 若有依賴的 pip 套件，請列出 `pip install ...` 命令。

## 跨專案整合規範
- 所有專案應遵循統一的介面設計模式。
- 使用 ROS 2 服務和主題進行專案間通訊。
- 配置文件統一放置在 `/app/config/` 目錄。
- 日誌格式統一，便於集中監控和分析。
- 錯誤代碼和狀態碼應保持一致性。

## 🔗 與根目錄指導文檔的關係

### 職責分工
- **根目錄 (/app/host/.augment-guidelines)**: 專注於部署、管理、系統整體架構
- **應用目錄 (app/.augment-guidelines)**: 專注於開發、技術細節、ROS 2 實作

### 交叉引用機制
- 系統部署和管理 → 參考 `/app/host/.augment-guidelines`
- Docker 容器管理 → 參考 `/app/host/README.md`
- 故障排除和監控 → 參考根目錄文檔 (`/app/host/.augment-guidelines`, `/app/host/README.md`)

### 避免內容重複原則
- 應用目錄不重複 Docker 部署指令
- 應用目錄不重複容器管理操作
- 應用目錄不重複系統架構總覽
- 專注於容器內的開發和技術細節

## 🤖 AI 助理環境識別指導

### ⚠️ 重要提醒：此文檔僅適用於容器內環境
**如果您在宿主機環境（`/home/ct/RosAGV`），請立即切換到根目錄的 `/app/host/.augment-guidelines` 文檔**

### 當前環境判斷方法
AI 助理必須首先確認當前執行環境，避免使用錯誤的指令格式：

#### 🔍 環境檢查指令
```bash
# 檢查當前工作目錄
pwd

# 檢查是否在容器內
if [ -f "/.dockerenv" ]; then
    echo "✅ 在 Docker 容器內"
else
    echo "❌ 在宿主機環境，請使用根目錄指導文檔 (/app/host/.augment-guidelines)"
fi

# 檢查 ROS 2 環境
if command -v ros2 > /dev/null; then
    echo "✅ 可直接執行 ros2 指令"
else
    echo "❌ 無法執行 ros2，可能在宿主機環境"
fi
```

#### 容器內環境特徵確認
**AGV 容器環境 (rosagv)**:
- 當前路徑：`/app`
- 可執行：`ros2`、`colcon`、`source setup.bash`
- 環境變數：`RMW_IMPLEMENTATION=rmw_zenoh_cpp`
- 可用函數：`build_all`、`test_all`、`check_system_status`

**AGVC 容器環境 (agvc_server)**:
- 當前路徑：`/app`
- 可執行：`ros2`、`colcon`、`psql`、`source setup.agvc.bash`
- 可用函數：`start_ecs`、`agvc_source`、`check_agvc_status`
- 額外服務：PostgreSQL、Nginx、pgAdmin4

### 指令選擇策略
**容器內環境專用指令格式**:
```bash
# AGV 容器環境 (rosagv)
source setup.bash                    # 載入開發環境
build_all                            # 建置所有工作空間
test_all                             # 測試所有工作空間
check_system_status                  # 檢查系統狀態
ros2 launch loader_agv launch.py    # 啟動 AGV 服務

# AGVC 容器環境 (agvc_server)
source setup.bash                   # 載入統一環境（自動檢測為 AGVC）
agvc_source                          # 載入 AGVC 專用工作空間
start_ecs                            # 啟動 ECS 服務
check_agvc_status                    # 檢查 AGVC 狀態
```

**❌ 絕對不要在容器內使用的指令**:
```bash
# 這些是宿主機指令，不要在容器內使用
docker compose -f docker-compose.yml up -d
docker compose exec rosagv bash
docker stats
```

### 環境切換指導
如果 AI 助理發現當前環境不正確：
```bash
# 如果在宿主機但需要容器內操作
echo "請先進入容器："
echo "docker compose -f docker-compose.yml exec rosagv bash"
echo "或"
echo "docker compose -f docker-compose.agvc.yml exec agvc_server bash"

# 如果在容器內但需要宿主機操作
echo "請退出容器回到宿主機："
echo "exit"
echo "然後參考根目錄的 /app/host/.augment-guidelines 文檔"
```

## 📋 開發工作流程最佳實踐

### 新工作空間開發流程
1. **建立工作空間結構**
2. **編寫 package.xml 和依賴聲明**
3. **實作核心功能**
4. **編寫測試案例**
5. **更新工作空間 README.md**
6. **更新應用目錄 README.md 狀態表格**
7. **如需要，更新根目錄 README.md (/app/host/README.md)**

### 依賴關係變更流程
1. **修改 package.xml 依賴聲明**
2. **更新 Python import 語句**
3. **測試依賴關係**
4. **更新 .augment-guidelines 中的依賴說明**
5. **更新相關 README.md 文檔（包括 /app/host/README.md）**

### 啟動腳本修改流程
1. **修改 startup.agv.bash 或 startup.agvc.bash**
2. **測試容器啟動流程**
3. **更新應用目錄 README.md 中的啟動狀態標記**
4. **更新根目錄 README.md (/app/host/README.md) 中的啟動流程說明**
5. **更新 .augment-guidelines 中的工作空間狀態**

---

**系統部署和管理指導請參考**: `/app/host/.augment-guidelines`
**容器管理和故障排除請參考**: `/app/host/README.md`
**具體工作空間技術細節請參考**: `*_ws/README.md`

