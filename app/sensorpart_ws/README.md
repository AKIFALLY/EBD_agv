# 感測器工作空間 (sensorpart_ws)

## 📋 基本資訊

**啟動狀態**: ⚠️ 手動啟動 (未在容器啟動腳本中自動啟動)
**運行環境**: 🚗 AGV 車載系統 (主要)
**主要功能**: TCP 感測器通訊 - 相機定位和 OCR 資料接收
**依賴狀態**: 使用系統套件，無其他工作空間依賴
**手動啟動**: 可使用 `ros2 run sensorpart test_sensorpart_node` 啟動

## 📋 專案概述

感測器工作空間是 RosAGV 系統的感測器整合核心，專門負責與外部感測器設備的 TCP 通訊。該工作空間實現了與相機系統的 TCP 連線，支援 3D 定位資料和 OCR 結果的即時接收和處理，為 AGV 系統提供重要的環境感知和識別能力。

作為 AGV 車載系統的重要組件，SensorPart 提供了完整的 TCP 通訊邏輯，包括自動重連機制、資料解析處理、ROS 2 節點整合等。系統採用多執行緒設計，支援即時資料接收和處理，並提供完整的錯誤處理和連線管理機制。

**重要特點**: 實現了完整的 SensorPart TCP 客戶端和 ROS 2 節點整合，支援與相機系統 (192.168.2.100:2005) 的即時通訊，並提供 3D 定位和 OCR 資料的自動解析功能。

## 🔗 依賴關係

### 系統套件依賴
- **rclpy**: ROS 2 Python 客戶端庫
- **setuptools**: Python 套件建置工具
- **socket**: Python 標準庫 TCP 通訊
- **threading**: Python 標準庫多執行緒支援
- **re**: Python 標準庫正規表達式

### 依賴的工作空間
- **無**: 此工作空間為獨立模組，不依賴其他工作空間

### 被依賴的工作空間
- **agv_ws**: 可能使用感測器資料進行 AGV 控制 (🚗 AGV 車載系統)

### 外部依賴
- **相機系統**: 連線到 192.168.2.100:2005 的相機 TCP 服務

## 🏗️ 專案結構

```
sensorpart_ws/
├── src/                           # 原始碼
│   └── sensorpart/               # 感測器套件
│       ├── sensorpart/
│       │   ├── __init__.py       # 套件初始化
│       │   ├── sensorpart.py     # TCP 感測器客戶端 (核心實作)
│       │   └── test_sensorpart_node.py # ROS 2 測試節點 (完整實作)
│       ├── test/                 # 測試檔案
│       │   ├── test_copyright.py # 版權測試
│       │   ├── test_flake8.py    # 程式碼風格測試
│       │   └── test_pep257.py    # 文檔字串測試
│       ├── resource/             # 資源檔案
│       │   └── sensorpart        # 套件資源標記
│       ├── package.xml           # 套件配置
│       ├── setup.py              # Python 套件設定 (僅系統套件)
│       └── setup.cfg             # 建置配置
├── build/                         # 建置輸出目錄
├── install/                       # 安裝目錄
└── log/                          # 日誌目錄
```

## ⚙️ 主要功能

### 1. SensorPart TCP 客戶端 (sensorpart.py)
**TCP 通訊核心**:
- **自動連線**: 自動連線到相機系統 (192.168.2.100:2005)
- **自動重連**: 連線失敗時自動重試機制 (5 秒間隔)
- **多執行緒處理**: 使用 threading 進行非阻塞資料接收
- **優雅關閉**: 完整的停止和清理機制

**資料處理功能**:
- **3D 定位資料**: 解析相機提供的 3D 定位資訊
- **OCR 結果處理**: 接收和處理 OCR 識別結果
- **資料解析**: 使用正規表達式解析接收的資料
- **狀態管理**: 維護連線狀態和資料狀態

### 2. ROS 2 測試節點 (test_sensorpart_node.py)
**ROS 2 整合**:
- **節點架構**: 標準的 ROS 2 節點實作
- **定時器回調**: 1 秒週期的資料日誌輸出
- **TCP 客戶端整合**: 整合 SensorPart TCP 客戶端
- **優雅關閉**: 節點關閉時自動停止 TCP 客戶端

**核心方法**:
- `timer_callback()`: 定期輸出位置資料和 OCR 結果
- `destroy_node()`: 節點銷毀時的清理處理

### 3. TCP 通訊協定
**連線參數**:
- **預設主機**: 192.168.2.100
- **預設端口**: 2005
- **協定**: TCP Socket 通訊
- **編碼**: 文字資料傳輸

**資料格式**:
- **position_data**: 3D 定位資料 (從相機系統接收)
- **ocr_result**: OCR 識別結果 (從相機系統接收)

## 🔧 核心 API

### SensorPart TCP 客戶端使用
```python
from sensorpart.sensorpart import SensorPart

# 建立 TCP 客戶端 (使用預設參數)
sensor = SensorPart()

# 建立 TCP 客戶端 (自訂參數)
sensor = SensorPart(host='192.168.2.100', port=2005)

# 啟動 TCP 客戶端 (自動連線和資料接收)
sensor.start()

# 取得 3D 定位資料
position_data = sensor.position_data

# 取得 OCR 結果
ocr_result = sensor.ocr_result

# 檢查連線狀態
if sensor.is_connected:
    print("TCP 連線正常")

# 停止 TCP 客戶端
sensor.stop()
```

### ROS 2 節點使用
```python
import rclpy
from sensorpart.test_sensorpart_node import TestSensorPartNode

# 初始化 ROS 2
rclpy.init()

# 建立感測器節點
node = TestSensorPartNode()

# 啟動節點 (會自動啟動 TCP 客戶端)
try:
    rclpy.spin(node)
except KeyboardInterrupt:
    node.get_logger().info("正在關閉感測器節點...")
finally:
    node.destroy_node()
    rclpy.shutdown()
```

### 直接執行範例
```bash
# 執行 ROS 2 測試節點
ros2 run sensorpart test_sensorpart_node

# 執行 TCP 客戶端 (如果有 main 函數)
ros2 run sensorpart sensorpart
```

## 🧪 測試方法

### 1. 建置和測試
```bash
# 建置工作空間
source /opt/ros/jazzy/setup.bash && source /opt/ws_rmw_zenoh/install/setup.bash && cd /app/sensorpart_ws && colcon build
source install/setup.bash

# 執行測試
colcon test
colcon test-result --verbose
```

### 2. 系統套件測試
```bash
# 測試 ROS 2 相關套件
python3 -c "
import rclpy
print('✅ rclpy 可用')
print(f'rclpy 位置: {rclpy.__file__}')
"

# 測試 Python 標準庫
python3 -c "
import socket
import threading
import re
print('✅ Python 標準庫可用')
print('socket, threading, re 模組正常')
"
```

### 3. SensorPart 模組測試
```bash
# 測試 SensorPart 模組載入
python3 -c "
from sensorpart.sensorpart import SensorPart
from sensorpart.test_sensorpart_node import TestSensorPartNode
print('✅ SensorPart 模組載入成功')
"

# 測試 TCP 客戶端初始化
python3 -c "
from sensorpart.sensorpart import SensorPart
sensor = SensorPart(host='192.168.2.100', port=2005)
print('✅ SensorPart TCP 客戶端初始化成功')
print(f'主機: {sensor.host}, 端口: {sensor.port}')
"
```

### 4. ROS 2 節點測試
```bash
# 測試 ROS 2 節點啟動
ros2 run sensorpart test_sensorpart_node &
sleep 5

# 檢查節點狀態
ros2 node list | grep sensorpart

# 檢查節點資訊
ros2 node info /sensorpart_node

# 停止節點
pkill -f test_sensorpart_node
```

### 5. TCP 連線測試
```bash
# 測試 TCP 連線 (需要相機系統運行)
python3 -c "
from sensorpart.sensorpart import SensorPart
import time

sensor = SensorPart(host='192.168.2.100', port=2005)
print('正在測試 TCP 連線...')

try:
    sensor.start()
    time.sleep(5)  # 等待連線和資料接收

    if sensor.is_connected:
        print('✅ TCP 連線成功')
        print(f'位置資料: {sensor.position_data}')
        print(f'OCR 結果: {sensor.ocr_result}')
    else:
        print('❌ TCP 連線失敗')

    sensor.stop()
except Exception as e:
    print(f'❌ TCP 連線測試失敗: {e}')
"
```

## 🚀 使用方法

### 1. 建置工作空間
```bash
# 載入 ROS 2 環境並建置
source /opt/ros/jazzy/setup.bash && source /opt/ws_rmw_zenoh/install/setup.bash && cd /app/sensorpart_ws && colcon build
source install/setup.bash
```

### 2. 啟動 SensorPart 節點 (手動啟動)
```bash
# 方法 1: 啟動 ROS 2 測試節點
ros2 run sensorpart test_sensorpart_node

# 方法 2: 直接執行 TCP 客戶端 (如果有 main 函數)
ros2 run sensorpart sensorpart
```

### 3. 檢查 SensorPart 狀態
```bash
# 檢查 SensorPart 進程
ps aux | grep sensorpart

# 檢查 ROS 2 節點
ros2 node list | grep sensorpart

# 檢查節點資訊
ros2 node info /sensorpart_node
```

### 4. 監控感測器資料
```bash
# 查看節點日誌輸出 (會顯示位置資料和 OCR 結果)
ros2 run sensorpart test_sensorpart_node

# 預期輸出範例:
# [INFO] [sensorpart_node]: Position Data: {...}
# [INFO] [sensorpart_node]: OCR Result: {...}
```

### 5. 停止 SensorPart 服務
```bash
# 使用 Ctrl+C 優雅關閉
# 或強制終止
pkill -f sensorpart

# 檢查是否已停止
ps aux | grep sensorpart
```

## 🔧 故障排除

### 常見問題

#### 1. SensorPart 節點啟動失敗
**症狀**: `ros2 run sensorpart test_sensorpart_node` 無法啟動或立即退出
**解決方法**:
```bash
# 檢查套件建置狀態
ls -la /app/sensorpart_ws/install/

# 重新建置工作空間
cd /app/sensorpart_ws
rm -rf build install log
colcon build

# 檢查 setup.bash 載入
source install/setup.bash
ros2 pkg list | grep sensorpart

# 手動啟動並檢查錯誤
python3 -m sensorpart.test_sensorpart_node
```

#### 2. TCP 連線失敗
**症狀**: "Connection failed" 或無法連線到相機系統
**解決方法**:
```bash
# 檢查網路連線
ping 192.168.2.100

# 檢查端口是否開放
telnet 192.168.2.100 2005

# 測試 TCP 連線
python3 -c "
import socket
try:
    sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    sock.settimeout(5)
    result = sock.connect_ex(('192.168.2.100', 2005))
    if result == 0:
        print('✅ TCP 端口可連線')
    else:
        print('❌ TCP 端口無法連線')
    sock.close()
except Exception as e:
    print(f'❌ 連線測試失敗: {e}')
"
```

#### 3. 資料接收問題
**症狀**: TCP 連線成功但無法接收資料或資料為 None
**解決方法**:
```bash
# 檢查相機系統是否正在發送資料
# 手動測試 TCP 接收
python3 -c "
import socket
import time

sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
try:
    sock.connect(('192.168.2.100', 2005))
    sock.settimeout(10)
    data = sock.recv(1024)
    if data:
        print(f'✅ 接收到資料: {data.decode()}')
    else:
        print('❌ 未接收到資料')
except Exception as e:
    print(f'❌ 資料接收測試失敗: {e}')
finally:
    sock.close()
"
```

### 除錯工具
```bash
# 檢查 SensorPart 相關進程
ps aux | grep -E "(sensorpart|tcp)"

# 監控網路連線
netstat -tulpn | grep 2005

# 檢查 ROS 2 節點狀態
ros2 node list
ros2 node info /sensorpart_node
```

## 🔧 配置說明

### TCP 連線配置
```python
# SensorPart 預設配置
DEFAULT_CONFIG = {
    "host": "192.168.2.100",  # 相機系統 IP 位址
    "port": 2005,              # 相機系統 TCP 端口
    "timeout": 5,              # 連線超時時間 (秒)
    "retry_interval": 5,       # 重連間隔時間 (秒)
    "buffer_size": 1024        # TCP 接收緩衝區大小
}
```

### ROS 2 節點配置
```python
# TestSensorPartNode 配置
NODE_CONFIG = {
    "node_name": "sensorpart_node",
    "timer_period": 1.0,       # 定時器週期 (秒)
    "log_level": "INFO"        # 日誌等級
}
```

### 資料格式配置
```python
# 資料處理配置
DATA_CONFIG = {
    "position_data": {
        "format": "3D coordinates",
        "encoding": "utf-8",
        "parser": "regex"
    },
    "ocr_result": {
        "format": "text string",
        "encoding": "utf-8",
        "parser": "regex"
    }
}
```

## 🔧 維護注意事項

1. **TCP 連線穩定性**: 定期檢查與相機系統的網路連線
2. **資料接收監控**: 監控 3D 定位和 OCR 資料的接收狀態
3. **錯誤處理機制**: 確保自動重連機制正常運作
4. **效能監控**: 監控 TCP 通訊和資料處理效能
5. **相機系統相容性**: 確保與相機系統 (192.168.2.100:2005) 的協定相容

## 📝 開發指南

### 擴展 TCP 通訊功能
1. 修改 `sensorpart.py` 中的資料解析邏輯
2. 新增新的資料格式支援
3. 更新 ROS 2 節點的資料處理
4. 測試新功能的穩定性

### 整合新的感測器
1. 參考 SensorPart 類別的設計模式
2. 實現新的 TCP 客戶端類別
3. 建立對應的 ROS 2 節點
4. 新增測試和文檔

## 📋 ToDo 清單

### 🔴 高優先級 (緊急)
- [x] **TCP 通訊功能完整實作** ✅ **已完成**
  - [x] 完整的相機 TCP 通訊介面 (SensorPart 類別)
  - [x] 3D 定位資料接收和解析
  - [x] OCR 結果處理和儲存
  - [x] 自動重連機制和錯誤處理
- [x] **ROS 2 節點整合** ✅ **已完成**
  - [x] TestSensorPartNode ROS 2 節點實作
  - [x] TCP 客戶端與 ROS 2 節點整合
  - [x] 定時器回調和資料日誌輸出
  - [x] 優雅關閉和清理機制

### 🟡 中優先級 (重要)
- [ ] **感測器功能擴展** (3 週)
  - [x] 基本 TCP 感測器已實現
  - [ ] 新增更多感測器類型支援 (雷射、視覺、距離)
  - [ ] 實現感測器管理器架構
  - [ ] 建立統一的感測器介面
- [ ] **資料處理增強** (2 週)
  - [x] 基本資料接收和解析已實現
  - [ ] 新增資料驗證和過濾機制
  - [ ] 實現資料融合演算法
  - [ ] 建立資料校準功能
- [ ] **測試覆蓋擴展** (2 週)
  - [x] 基本模組測試已建立
  - [ ] 新增感測器模擬器
  - [ ] 實現自動化整合測試
  - [ ] 建立效能和壓力測試

### 🟢 低優先級 (改善)
- [ ] **進階資料處理功能** (3 週)
  - [ ] 實現濾波處理演算法
  - [ ] 新增特徵提取功能
  - [ ] 建立模式識別系統
- [ ] **Web 監控介面整合** (2 週)
  - [ ] 與 web_api_ws 整合感測器狀態顯示
  - [ ] 新增即時資料視覺化
  - [ ] 建立感測器警報系統

### 🔧 技術債務
- [x] **基礎架構** ✅ **已完成**
  - [x] 標準的 ROS 2 套件架構
  - [x] 完整的 TCP 客戶端實作
  - [x] 多執行緒和錯誤處理機制
- [ ] **程式碼品質提升** (1 週)
  - [ ] 新增更完整的類型提示
  - [ ] 實現程式碼風格統一
  - [ ] 新增詳細的程式碼註解

### 📊 完成度追蹤 (基於實際程式碼分析)
- **TCP 通訊**: 95% ✅ (完整實作，包含自動重連)
- **ROS 2 節點整合**: 90% ✅ (完整的節點架構已實現)
- **資料處理**: 80% ✅ (基本解析和儲存已實現)
- **錯誤處理**: 85% ✅ (完整的異常處理機制)
- **測試覆蓋**: 60% 🔄 (基本測試已建立)
- **文檔完整性**: 95% ✅ (完整的技術文檔已完成)

### 🎯 里程碑 (更新版)
1. **v1.0.0** ✅ **已達成** - 核心 TCP 通訊功能實現
   - [x] SensorPart TCP 客戶端完整實作
   - [x] ROS 2 節點整合完成
   - [x] 3D 定位和 OCR 資料處理

2. **v1.1.0** (3 週後) - 功能擴展和測試
   - [ ] 感測器功能擴展
   - [ ] 資料處理增強
   - [ ] 測試覆蓋擴展

3. **v2.0.0** (8 週後) - 進階功能和整合
   - [ ] 進階資料處理功能
   - [ ] Web 監控介面整合
   - [ ] 多感測器支援

### 🏆 重要成就 (基於實際程式碼分析)
- ✅ **完整的 TCP 通訊系統**: 與相機系統的穩定 TCP 連線
- ✅ **自動重連機制**: 連線失敗時的自動重試功能
- ✅ **ROS 2 整合**: 標準的 ROS 2 節點架構和整合
- ✅ **多執行緒設計**: 非阻塞的資料接收和處理
- ✅ **資料解析功能**: 3D 定位和 OCR 資料的自動解析
