# AGV 測試資料生成工具

此目錄包含用於生成 AGV 狀態測試資料的工具腳本，用於開發和測試環境。

## 📋 腳本清單

### generate-agv-test-data.sh
- **用途**: 生成基本的 AGV 狀態測試資料
- **輸出**: 為 loader01、loader02、unloader01、cargo01 生成狀態檔案
- **檔案位置**: `/tmp/agv_status_*.json`

### generate-agv-test-data-complete.sh
- **用途**: 生成完整的測試資料集
- **特點**: 包含所有 AGV 類型的完整狀態資料

### generate-test-with-alarms.sh
- **用途**: 生成帶有警報的測試資料
- **特點**: 為 loader02 添加 6 個活動警報
- **警報**: ALARM_STATUS_1, 5, 12, 25, 48, 72

### generate-varied-alarms.sh
- **用途**: 生成多樣化警報模式的測試資料
- **模式**:
  - `none`: 無警報
  - `few`: 3-5 個隨機警報
  - `many`: 10-15 個隨機警報
  - `critical`: 關鍵警報（特定編號）
  - `sequential`: 前 10 個連續警報

## 🚀 使用方式

這些腳本必須在 **AGV 容器內** 執行：

```bash
# 從宿主機執行
docker compose -f docker-compose.yml exec rosagv bash /app/scripts/test-data/generate-agv-test-data.sh

# 或進入容器後執行
docker compose -f docker-compose.yml exec rosagv bash
cd /app/scripts/test-data
./generate-agv-test-data.sh
```

## 📊 輸出檔案

生成的測試資料檔案位於 `/tmp/` 目錄：
- `/tmp/agv_status.json` - 主要狀態檔案
- `/tmp/agv_status_loader01.json` - Loader 01 狀態
- `/tmp/agv_status_loader02.json` - Loader 02 狀態
- `/tmp/agv_status_unloader01.json` - Unloader 01 狀態
- `/tmp/agv_status_cargo01.json` - Cargo 01 狀態

## 💡 使用場景

1. **前端開發**: 不需要實際 AGV 硬體即可開發和測試 UI
2. **警報測試**: 測試不同警報狀態的顯示和處理
3. **多車測試**: 模擬多台 AGV 同時運行的場景
4. **系統整合**: 測試與其他系統的整合功能

## ⚠️ 注意事項

- 這些是測試工具，不應在生產環境使用
- 生成的資料會覆蓋 `/tmp/` 中的同名檔案
- 確保在 AGV 容器內執行，否則會出錯

## 📅 維護資訊

- **最後更新**: 2025-09-16
- **維護狀態**: 活躍使用中
- **相關系統**: agv_node_base.py, AGV UI 系統